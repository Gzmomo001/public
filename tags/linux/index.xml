<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>Linux on Henry Zhang</title>
        <link>http://localhost:1313/tags/linux/</link>
        <description>Recent content in Linux on Henry Zhang</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>en-us</language>
        <copyright>blog@mail.henry070.org</copyright>
        <lastBuildDate>Mon, 02 Mar 2026 13:07:53 +0800</lastBuildDate><atom:link href="http://localhost:1313/tags/linux/index.xml" rel="self" type="application/rss+xml" /><item>
        <title>软件包管理：Linux 的应用商店</title>
        <link>http://localhost:1313/p/%E8%BD%AF%E4%BB%B6%E5%8C%85%E7%AE%A1%E7%90%86linux-%E7%9A%84%E5%BA%94%E7%94%A8%E5%95%86%E5%BA%97/</link>
        <pubDate>Sun, 11 Jan 2026 03:00:00 +0800</pubDate>
        
        <guid>http://localhost:1313/p/%E8%BD%AF%E4%BB%B6%E5%8C%85%E7%AE%A1%E7%90%86linux-%E7%9A%84%E5%BA%94%E7%94%A8%E5%95%86%E5%BA%97/</guid>
        <description>&lt;img src="http://localhost:1313/default.jpeg" alt="Featured image of post 软件包管理：Linux 的应用商店" /&gt;&lt;h1 id=&#34;软件包管理linux-的应用商店&#34;&gt;软件包管理：Linux 的应用商店
&lt;/h1&gt;&lt;h2 id=&#34;引言&#34;&gt;引言
&lt;/h2&gt;&lt;p&gt;软件包管理是 Linux 系统中最日常的任务之一。不同的发行版使用不同的包管理器，但核心概念是相通的。在这篇文章中，我们将系统学习 Linux 的软件包管理系统。&lt;/p&gt;
&lt;h2 id=&#34;包管理器概述&#34;&gt;包管理器概述
&lt;/h2&gt;&lt;h3 id=&#34;什么是软件包&#34;&gt;什么是软件包
&lt;/h3&gt;&lt;p&gt;软件包是预编译的应用程序及其元数据的打包格式：&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart TD
    A[源代码] --&amp;gt; B[编译]
    B --&amp;gt; C[打包]
    C --&amp;gt; D[软件包]

    D --&amp;gt; E[Debian 包&amp;lt;br/&amp;gt;.deb]
    D --&amp;gt; F[Red Hat 包&amp;lt;br/&amp;gt;.rpm]
    D --&amp;gt; G[Arch 包&amp;lt;br/&amp;gt;.pkg.tar.zst]

    E --&amp;gt; H[APT]
    F --&amp;gt; I[DNF/YUM]
    G --&amp;gt; J[Pacman]

    style H fill:#e3f2fd
    style I fill:#fff3e0
    style J fill:#f3e5f5
&lt;/pre&gt;

&lt;h3 id=&#34;包管理器的功能&#34;&gt;包管理器的功能
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;安装软件&lt;/strong&gt;：从仓库或本地文件安装&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;卸载软件&lt;/strong&gt;：删除软件及其配置&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;更新软件&lt;/strong&gt;：升级到最新版本&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;依赖管理&lt;/strong&gt;：自动处理依赖关系&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;仓库管理&lt;/strong&gt;：添加、删除、更新软件源&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;debian-系apt&#34;&gt;Debian 系：APT
&lt;/h2&gt;&lt;h3 id=&#34;apt-基础&#34;&gt;APT 基础
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 更新软件包列表
sudo apt update

# 升级所有已安装的软件包
sudo apt upgrade

# 完整系统升级（包括依赖关系变化）
sudo apt full-upgrade

# 安装软件
sudo apt install nginx
sudo apt install vim git curl

# 删除软件（保留配置）
sudo apt remove nginx

# 删除软件和配置
sudo apt purge nginx

# 删除不需要的依赖
sudo apt autoremove

# 清理下载的包文件
sudo apt clean
sudo apt autoclean&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;软件源配置&#34;&gt;软件源配置
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 软件源文件
/etc/apt/sources.list
/etc/apt/sources.list.d/*.conf

# 查看当前源
cat /etc/apt/sources.list

# 添加 PPA（Personal Package Archive）
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt update

# 手动添加源
echo &amp;#39;deb https://example.com/ubuntu jammy main&amp;#39; | sudo tee /etc/apt/sources.list.d/custom.list

# 添加 GPG 密钥
wget -qO - https://example.com/key.gpg | sudo apt-key add -

# 更新系统后清理旧包
sudo apt autoremove
sudo apt autoclean&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;deb-文件&#34;&gt;.deb 文件
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 安装 .deb 文件
sudo dpkg -i package.deb

# 修复依赖问题
sudo apt install -f

# 查看已安装的包
dpkg -l | grep nginx

# 查看包信息
dpkg -I package.deb

# 查看包内容
dpkg -c package.deb

# 卸载 .deb 包
sudo dpkg -r package_name
sudo dpkg -P package_name  # 包括配置文件&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;常用操作&#34;&gt;常用操作
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 搜索软件包
apt search nginx
apt show nginx           # 查看包详情

# 查看已安装的包
apt list --installed

# 查看可升级的包
apt list --upgradable

# 查看包依赖关系
apt depends nginx
apt rdepends nginx       # 反向依赖

# 下载但不安装
apt download nginx
apt source nginx         # 下载源代码

# 固定软件版本
sudo apt mark hold nginx     # 禁止升级
sudo apt mark unhold nginx   # 允许升级&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;red-hat-系dnf&#34;&gt;Red Hat 系：DNF
&lt;/h2&gt;&lt;h3 id=&#34;dnf-基础&#34;&gt;DNF 基础
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 安装软件
sudo dnf install nginx

# 更新软件
sudo dnf update nginx
sudo dnf upgrade              # 升级所有软件

# 删除软件
sudo dnf remove nginx
sudo dnf erase nginx

# 查找软件
dnf search nginx

# 查看包信息
dnf info nginx&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;仓库配置&#34;&gt;仓库配置
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 仓库配置目录
/etc/yum.repos.d/*.repo

# 查看已启用的仓库
dnf repolist

# 查看所有仓库
dnf repolist all

# 启用/禁用仓库
sudo dnf config-manager --set-enabled epel
sudo dnf config-manager --set-disabled epel

# 添加仓库
sudo dnf config-manager --add-repo https://download.example.com/repo.rpm

# EPEL（Extra Packages for Enterprise Linux）
sudo dnf install epel-release&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;rpm-文件&#34;&gt;.rpm 文件
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 安装 .rpm 文件
sudo rpm -ivh package.rpm
# 或
sudo dnf localinstall package.rpm

# 查询已安装的包
rpm -qa | grep nginx
rpm -qi nginx             # 包信息
rpm -ql nginx             # 包文件列表
rpm -qf /bin/nginx        # 查看文件属于哪个包

# 卸载 .rpm 包
sudo rpm -e nginx

# 验证包
rpm -V nginx&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;arch-系pacman&#34;&gt;Arch 系：Pacman
&lt;/h2&gt;&lt;h3 id=&#34;pacman-基础&#34;&gt;Pacman 基础
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 同步包数据库
sudo pacman -Sy

# 升级系统
sudo pacman -Syu

# 安装软件
sudo pacman -S nginx

# 删除软件
sudo pacman -R nginx          # 保留依赖和配置
sudo pacman -Rs nginx         # 删除依赖
sudo pacman -Rns nginx        # 删除依赖和配置

# 搜索软件
pacman -Ss nginx

# 查看包信息
pacman -Si nginx
pacman -Qii nginx             # 已安装包的详细信息&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;仓库和-aur&#34;&gt;仓库和 AUR
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 官方仓库
core - 核心软件
extra - 额外软件
community - 社区软件

# 查看包在哪个仓库
pacman -Ss nginx

# AUR（Arch User Repository）
# 需要使用 yay 或 paru 等 AUR 助手

# 安装 yay
sudo pacman -S base-devel git
git clone https://aur.archlinux.org/yay.git
cd yay
makepkg -si

# 使用 yay
yay -S google-chrome         # 从 AUR 安装
yay -Syu                     # 升级系统（包括 AUR）
yay -Ss package             # 搜索包&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;pkgtarzst-文件&#34;&gt;.pkg.tar.zst 文件
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 安装本地包文件
sudo pacman -U package.pkg.tar.zst

# 查看包内容
pacman -Ql package
pacman -Qo /path/to/file    # 文件属于哪个包&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;依赖管理&#34;&gt;依赖管理
&lt;/h2&gt;&lt;h3 id=&#34;依赖类型&#34;&gt;依赖类型
&lt;/h3&gt;&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart TD
    A[软件包] --&amp;gt; B[依赖]
    B --&amp;gt; C[硬依赖&amp;lt;br/&amp;gt;requires]
    B --&amp;gt; D[软依赖&amp;lt;br/&amp;gt;suggests]
    B --&amp;gt; E[构建依赖&amp;lt;br/&amp;gt;build-dep]

    C --&amp;gt; F[必须安装]
    D --&amp;gt; G[推荐安装]
    E --&amp;gt; H[编译时需要]

    style C fill:#ffcdd2
    style D fill:#fff3e0
    style E fill:#e3f2fd
&lt;/pre&gt;

&lt;h3 id=&#34;查看依赖关系&#34;&gt;查看依赖关系
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Debian/Ubuntu
apt depends nginx           # 正向依赖
apt rdepends nginx          # 反向依赖（被依赖）

# Red Hat/CentOS
dnf repoquery --requires nginx
dnf repoquery --whatrequires nginx

# Arch
pactree nginx               # 依赖树
pactree -r nginx            # 反向依赖树&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;解决依赖冲突&#34;&gt;解决依赖冲突
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看冲突的包
apt show package | grep Conflicts

# 强制安装（不推荐）
sudo apt install -f

# 使用特定版本
sudo apt install package=1.0.0

# 锁定版本
sudo apt-mark hold package&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;snap-和-flatpak&#34;&gt;Snap 和 Flatpak
&lt;/h2&gt;&lt;h3 id=&#34;snapcanonical&#34;&gt;Snap（Canonical）
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 安装 snapd
sudo apt install snapd

# 查找 snap 应用
snap find chrome

# 安装 snap
sudo snap install chromium
sudo snap install chromium --classic  # 经典模式

# 列出已安装的 snap
snap list

# 更新 snap
sudo snap refresh chromium

# 删除 snap
sudo snap remove chromium

# 查看 snap 信息
snap info chromium&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;flatpak通用&#34;&gt;Flatpak（通用）
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 安装 flatpak
sudo apt install flatpak

# 添加 Flathub 仓库
flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo

# 查找应用
flatpak search spotify

# 安装应用
flatpak install flathub com.spotify.Client

# 列出已安装的应用
flatpak list

# 更新应用
flatpak update com.spotify.Client

# 删除应用
flatpak uninstall com.spotify.Client

# 运行应用
flatpak run com.spotify.Client&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;appimage&#34;&gt;AppImage
&lt;/h2&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 下载 AppImage 文件
wget https://example.com/app.AppImage

# 赋予执行权限
chmod &amp;#43;x app.AppImage

# 运行
./app.AppImage

# 集成到系统（可选）
./app.AppImage --appimage-extract
./squashfs-root/AppRun

# 或使用 AppImageLauncher
sudo apt install appimagelauncher&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;源码编译&#34;&gt;源码编译
&lt;/h2&gt;&lt;h3 id=&#34;基本流程&#34;&gt;基本流程
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 1. 安装编译工具
sudo apt install build-essential

# 2. 下载源代码
wget https://example.com/source.tar.gz
tar -xf source.tar.gz
cd source

# 3. 配置
./configure --prefix=/usr/local

# 4. 编译
make

# 5. 安装
sudo make install

# 6. 卸载
sudo make uninstall&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;checkinstall创建包&#34;&gt;checkinstall（创建包）
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 安装 checkinstall
sudo apt install checkinstall

# 使用 checkinstall 替代 make install
make
sudo checkinstall

# 这会创建一个 .deb 包，便于管理&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;实用技巧&#34;&gt;实用技巧
&lt;/h2&gt;&lt;h3 id=&#34;查找命令对应的包&#34;&gt;查找命令对应的包
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Debian/Ubuntu
apt search command_name
apt-file search command_name     # 需要更新数据库：sudo apt update

# Red Hat/CentOS
dnf provides command_name
yum whatprovides &amp;#34;*bin/command&amp;#34;

# Arch
pacman -Fy /usr/bin/command      # 需要更新数据库：sudo pacman -Fy
pacman -F command&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;包管理器快捷方式&#34;&gt;包管理器快捷方式
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# APT
alias apt-upgrade=&amp;#39;sudo apt update &amp;amp;&amp;amp; sudo apt upgrade -y&amp;#39;
alias apt-install=&amp;#39;sudo apt install -y&amp;#39;
alias apt-search=&amp;#39;apt search&amp;#39;

# DNF
alias dnf-upgrade=&amp;#39;sudo dnf upgrade -y&amp;#39;
alias dnf-install=&amp;#39;sudo dnf install -y&amp;#39;

# Pacman
alias pac-upgrade=&amp;#39;sudo pacman -Syu&amp;#39;
alias pac-install=&amp;#39;sudo pacman -S&amp;#39;
alias pac-remove=&amp;#39;sudo pacman -Rns&amp;#39;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;清理系统&#34;&gt;清理系统
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Debian/Ubuntu
sudo apt autoremove      # 删除不需要的依赖
sudo apt autoclean       # 清理旧包文件
sudo apt clean           # 清理所有包文件

# Red Hat/CentOS
sudo dnf autoremove
sudo dnf clean all

# Arch
sudo pacman -Sc          # 清理未安装的包文件
sudo pacman -Scc         # 清理所有缓存&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;总结&#34;&gt;总结
&lt;/h2&gt;&lt;p&gt;软件包管理是 Linux 系统管理的日常任务。通过这篇文章，我们学习了：&lt;/p&gt;
&lt;h3 id=&#34;核心技能&#34;&gt;核心技能
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;APT&lt;/strong&gt;：Debian/Ubuntu 系统的包管理&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;DNF&lt;/strong&gt;：Red Hat/Fedora 系统的包管理&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Pacman&lt;/strong&gt;：Arch Linux 的包管理&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;通用格式&lt;/strong&gt;：Snap、Flatpak、AppImage&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;源码编译&lt;/strong&gt;：终极灵活性&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;最佳实践&#34;&gt;最佳实践
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;定期更新&lt;/strong&gt;：保持系统和软件最新&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;使用官方源&lt;/strong&gt;：安全可靠&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;理解依赖&lt;/strong&gt;：避免冲突&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;清理无用包&lt;/strong&gt;：节省空间&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;备份配置&lt;/strong&gt;：升级前备份重要配置&lt;/li&gt;
&lt;/ol&gt;
&lt;blockquote&gt;
&lt;p&gt;&amp;ldquo;包管理器是 Linux 最大的优势之一，它让软件安装变得简单。&amp;rdquo; — Linus Torvalds&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;掌握包管理器，让你的 Linux 之旅更加顺畅！&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;← 返回系列概述&lt;/strong&gt;: &lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/linux-%E6%A6%82%E8%BF%B0%E4%BB%8E-unix-%E5%88%B0%E5%BC%80%E6%BA%90%E9%9D%A9%E5%91%BD/&#34; &gt;Linux 概述：从 Unix 到开源革命&lt;/a&gt;&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;参考资源&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://wiki.debian.org/APT&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;APT 用户指南&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://dnf.readthedocs.io/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;DNF 文档&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://wiki.archlinux.org/title/pacman&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Pacman 手册&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://docs.flatpak.org/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Flatpak 文档&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
        </item>
        <item>
        <title>日志和监控：Linux 的体检医生</title>
        <link>http://localhost:1313/p/%E6%97%A5%E5%BF%97%E5%92%8C%E7%9B%91%E6%8E%A7linux-%E7%9A%84%E4%BD%93%E6%A3%80%E5%8C%BB%E7%94%9F/</link>
        <pubDate>Sun, 11 Jan 2026 02:00:00 +0800</pubDate>
        
        <guid>http://localhost:1313/p/%E6%97%A5%E5%BF%97%E5%92%8C%E7%9B%91%E6%8E%A7linux-%E7%9A%84%E4%BD%93%E6%A3%80%E5%8C%BB%E7%94%9F/</guid>
        <description>&lt;img src="http://localhost:1313/default.jpeg" alt="Featured image of post 日志和监控：Linux 的体检医生" /&gt;&lt;h1 id=&#34;日志和监控linux-的体检医生&#34;&gt;日志和监控：Linux 的体检医生
&lt;/h1&gt;&lt;h2 id=&#34;引言&#34;&gt;引言
&lt;/h2&gt;&lt;p&gt;日志和监控是 Linux 系统管理的眼睛和耳朵。通过日志，我们可以了解系统发生了什么；通过监控，我们可以知道系统正在做什么。在这篇文章中，我们将系统学习 Linux 的日志管理和系统监控。&lt;/p&gt;
&lt;h2 id=&#34;系统日志概述&#34;&gt;系统日志概述
&lt;/h2&gt;&lt;h3 id=&#34;日志的重要性&#34;&gt;日志的重要性
&lt;/h3&gt;&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart TD
    A[系统事件] --&amp;gt; B[记录日志]
    B --&amp;gt; C[故障排查]
    B --&amp;gt; D[性能分析]
    B --&amp;gt; E[安全审计]
    B --&amp;gt; F[合规要求]

    style B fill:#fff3e0
    style C fill:#ffebee
    style D fill:#e8f5e9
    style E fill:#fff3e0
    style F fill:#e3f2fd
&lt;/pre&gt;

&lt;h3 id=&#34;日志位置&#34;&gt;日志位置
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 传统 syslog 日志位置
/var/log/syslog          # 系统日志（Debian/Ubuntu）
/var/log/messages        # 系统日志（RHEL/CentOS）
/var/log/auth.log        # 认证日志
/var/log/kern.log        # 内核日志
/var/log/boot.log        # 启动日志
/var/log/dmesg           # 启动时的内核消息

# 应用日志
/var/log/nginx/          # Nginx 日志
/var/log/apache2/        # Apache 日志
/var/log/mysql/          # MySQL 日志&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;日志级别&#34;&gt;日志级别
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 严重程度从高到低：
0 - emerg   # 紧急，系统不可用
1 - alert   # 必须立即采取措施
2 - crit    # 严重情况
3 - err     # 错误
4 - warning # 警告
5 - notice  # 正常但重要
6 - info    # 信息性消息
7 - debug   # 调试信息&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;journald&#34;&gt;journald
&lt;/h2&gt;&lt;h3 id=&#34;journald-简介&#34;&gt;journald 简介
&lt;/h3&gt;&lt;p&gt;journald 是 systemd 提供的日志服务，替代了传统的 syslog：&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart LR
    A[应用] --&amp;gt;|日志| B[journald]
    B --&amp;gt;|存储| C[二进制日志]
    B --&amp;gt;|转发| D[rsyslog]
    D --&amp;gt;|存储| E[文本日志]

    style B fill:#fff3e0
    style C fill:#e3f2fd
    style E fill:#c8e6c9
&lt;/pre&gt;

&lt;h3 id=&#34;journalctl-命令&#34;&gt;journalctl 命令
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看所有日志
sudo journalctl

# 查看最新日志
sudo journalctl -e

# 实时跟踪日志
sudo journalctl -f

# 查看内核日志
sudo journalctl -k

# 查看特定单元的日志
sudo journalctl -u nginx
sudo journalctl -u sshd

# 查看最近1小时的日志
sudo journalctl --since &amp;#34;1 hour ago&amp;#34;

# 查看特定时间段的日志
sudo journalctl --since &amp;#34;2026-01-10 00:00:00&amp;#34; --until &amp;#34;2026-01-11 00:00:00&amp;#34;

# 按优先级过滤
sudo journalctl -p err        # 错误级别
sudo journalctl -p notice     # 通知及以上级别

# 显示详细的可读格式
sudo journalctl -o verbose
sudo journalctl -o json       # JSON 格式

# 限制输出行数
sudo journalctl -n 50         # 最近50行
sudo journalctl --since today | tail -n 100&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;持久化日志&#34;&gt;持久化日志
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 检查日志存储方式
sudo journalctl --header | grep Storage
# Storage: auto（默认，有 /var/log/journal 则持久化）

# 创建持久化存储
sudo mkdir -p /var/log/journal
sudo systemd-tmpfiles --create --prefix /var/log/journal
sudo systemctl restart systemd-journald

# 设置日志大小限制
sudo nano /etc/systemd/journald.conf
# SystemMaxUse=500M
# RuntimeMaxUse=100M&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;rsyslog&#34;&gt;rsyslog
&lt;/h2&gt;&lt;h3 id=&#34;rsyslog-简介&#34;&gt;rsyslog 简介
&lt;/h3&gt;&lt;p&gt;rsyslog 是传统的 syslog 守护进程，功能强大：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 检查 rsyslog 状态
sudo systemctl status rsyslog

# 配置文件
/etc/rsyslog.conf           # 主配置文件
/etc/rsyslog.d/*.conf       # 额外配置&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;配置示例&#34;&gt;配置示例
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# /etc/rsyslog.d/myapp.conf

# 记录特定应用的日志
if $programname == &amp;#39;myapp&amp;#39; then /var/log/myapp.log
&amp;amp; stop

# 记录到远程服务器
*.* @@192.168.1.100:514

# 根据严重程度记录
*.err /var/log/errors.log
*.warn /var/log/warnings.log&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;日志轮转logrotate&#34;&gt;日志轮转（logrotate）
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 配置文件
/etc/logrotate.conf          # 全局配置
/etc/logrotate.d/*           # 应用配置

# 查看特定配置
cat /etc/logrotate.d/nginx

# 示例配置
/var/log/nginx/*.log {
    daily                   # 每天轮转
    missingok               # 文件不存在不报错
    rotate 14               # 保留14天
    compress                # 压缩旧日志
    delaycompress           # 延迟一天压缩
    notifempty              # 空文件不轮转
    create 0640 www-data adm
    sharedscripts
    postrotate
        [ -f /var/run/nginx.pid ] &amp;amp;&amp;amp; kill -USR1 `cat /var/run/nginx.pid`
    endscript
}

# 手动执行
sudo logrotate -f /etc/logrotate.conf

# 调试模式
sudo logrotate -d /etc/logrotate.conf&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;系统监控工具&#34;&gt;系统监控工具
&lt;/h2&gt;&lt;h3 id=&#34;tophtop&#34;&gt;top/htop
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# top
top
# 按 P：按 CPU 排序
# 按 M：按内存排序
# 按 T：按时间排序
# 按 k：杀死进程
# 按 r：renice

# htop（更友好）
htop
# F1-F10：功能键
# /：搜索进程
# F9：调整优先级
# F6：排序方式&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;vmstat&#34;&gt;vmstat
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 虚拟内存统计
vmstat 2 5                 # 每2秒输出一次，共5次

# 输出解释：
# procs -----------memory---------- ---swap-- -----io---- -system-- ------cpu-----
# r  b   swpd   free   buff  cache   si   so    bi    bo   in   cs us sy id wa st
# 1  0      0 123456  12345 234567    0    0     2     3   10   20  5  2 92  1  0

# r: 等待运行的进程数
# b: 不可中断睡眠的进程数
# swpd: 使用的交换空间
# free: 空闲内存
# buff: 缓冲区
# cache: 缓存
# si/so: 换入/换出
# bi/bo: 块输入/输出
# cs: 上下文切换
# us/sy/id/wa: 用户/系统/空闲/等待 CPU 时间&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;iostat&#34;&gt;iostat
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 安装
sudo apt install sysstat

# I/O 统计
iostat -x 2 3

# 输出解释：
# avg-cpu:  %user   %nice %system %iowait  %steal   %idle
#            5.23    0.00    2.15    1.05    0.00   91.57

# Device:         rrqm/s   wrqm/s     r/s     w/s    rMB/s    wMB/s avgrq-sz avgqu-sz   await r_await w_await  svctm  %util
# sda               0.10     5.20   10.50   20.30     0.50     1.20    80.00     0.50   15.20   10.50   16.30   2.50   7.75

# rrqm/s: 每秒合并的读请求数
# wrqm/s: 每秒合并的写请求数
# r/s: 每秒读请求数
# w/s: 每秒写请求数
# await: 平均 I/O 等待时间
# %util: 设备利用率&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;pidstat&#34;&gt;pidstat
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 进程统计
pidstat 2 5                 # 每2秒输出一次，共5次

# CPU 使用
pidstat -u -p 1234 1

# 内存使用
pidstat -r -p 1234 1

# 上下文切换
pidstat -w -p 1234 1

# 线程统计
pidstat -t -p 1234 1&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;ssnetstat&#34;&gt;ss/netstat
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 网络统计
ss -s                      # 套接字摘要
ss -tunlp                  # 所有监听端口
ss -tunlp | grep :80       # 查找特定端口

# netstat（旧工具）
netstat -tunlp             # 所有监听端口
netstat -an                # 所有连接
netstat -s                 # 统计信息&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;性能分析&#34;&gt;性能分析
&lt;/h2&gt;&lt;h3 id=&#34;cpu-性能&#34;&gt;CPU 性能
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 检查 CPU 数量
nproc
lscpu

# 查看 CPU 使用率
top -bn1 | grep &amp;#34;Cpu(s)&amp;#34;
mpstat -P ALL 1

# 找出 CPU 消耗高的进程
ps aux --sort=-%cpu | head -10

# 查看每个 CPU 的负载
mpstat -P ALL 1 5

# 检查 CPU 亲和性
taskset -c -p 1234&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;内存性能&#34;&gt;内存性能
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看内存使用
free -h
cat /proc/meminfo

# 查看进程内存使用
ps aux --sort=-%mem | head -10
smem -s memory             # 更准确的内存统计

# 检查内存泄漏
valgrind --leak-check=full ./myapp

# 交换空间使用
swapon -s
vmstat 1 5 | grep si&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;磁盘-io&#34;&gt;磁盘 I/O
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看磁盘使用
df -h
du -sh * | sort -hr | head -10

# 磁盘 I/O 性能
iostat -x 1 5

# 查找 I/O 密集的进程
iotop
pidstat -d 1

# 磁盘速度测试
dd if=/dev/zero of=testfile bs=1G count=1 oflag=direct&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;网络性能&#34;&gt;网络性能
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 网络带宽
iftop                     # 实时带宽使用
nethogs                   # 按进程显示
speedtest-cli             # 网速测试

# 网络连接统计
ss -s
netstat -an | wc -l

# 抓包分析
tcpdump -i eth0 -w capture.pcap
wireshark capture.pcap&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;实战脚本&#34;&gt;实战脚本
&lt;/h2&gt;&lt;h3 id=&#34;系统健康检查&#34;&gt;系统健康检查
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash
# health_check.sh

echo &amp;#34;=== System Health Check ===&amp;#34;
echo &amp;#34;Time: $(date)&amp;#34;
echo &amp;#34;&amp;#34;

# CPU 使用率
echo &amp;#34;--- CPU Usage ---&amp;#34;
CPU_IDLE=$(top -bn1 | grep &amp;#34;Cpu(s)&amp;#34; | awk &amp;#39;{print $8}&amp;#39; | cut -d&amp;#39;%&amp;#39; -f1)
CPU_USED=$(echo &amp;#34;100 - $CPU_IDLE&amp;#34; | bc)
echo &amp;#34;CPU Used: ${CPU_USED}%&amp;#34;

if (( $(echo &amp;#34;$CPU_USED &amp;gt; 80&amp;#34; | bc -l) )); then
    echo &amp;#34;⚠️  WARNING: High CPU usage!&amp;#34;
fi

# 内存使用
echo &amp;#34;&amp;#34;
echo &amp;#34;--- Memory Usage ---&amp;#34;
MEM_INFO=$(free -m | awk &amp;#39;NR==2{printf &amp;#34;%.2f&amp;#34;, $3*100/$2}&amp;#39;)
echo &amp;#34;Memory Used: ${MEM_INFO}%&amp;#34;

if (( $(echo &amp;#34;$MEM_INFO &amp;gt; 80&amp;#34; | bc -l) )); then
    echo &amp;#34;⚠️  WARNING: High memory usage!&amp;#34;
fi

# 磁盘使用
echo &amp;#34;&amp;#34;
echo &amp;#34;--- Disk Usage ---&amp;#34;
df -h | grep -E &amp;#39;^/dev/&amp;#39; | while read line; do
    USAGE=$(echo $line | awk &amp;#39;{print $5}&amp;#39; | cut -d&amp;#39;%&amp;#39; -f1)
    MOUNT=$(echo $line | awk &amp;#39;{print $6}&amp;#39;)

    echo &amp;#34;$MOUNT: ${USAGE}%&amp;#34;

    if [ $USAGE -gt 80 ]; then
        echo &amp;#34;⚠️  WARNING: $MOUNT is ${USAGE}% full!&amp;#34;
    fi
done

# 系统负载
echo &amp;#34;&amp;#34;
echo &amp;#34;--- System Load ---&amp;#34;
UPTIME=$(uptime | awk -F&amp;#39;load average:&amp;#39; &amp;#39;{print $2}&amp;#39;)
echo &amp;#34;Load Average:$UPTIME&amp;#34;

# 错误日志
echo &amp;#34;&amp;#34;
echo &amp;#34;--- Recent Errors ---&amp;#34;
journalctl -p err -n 5 --no-pager&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;性能监控脚本&#34;&gt;性能监控脚本
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash
# performance_monitor.sh

OUTPUT_FILE=&amp;#34;performance_$(date &amp;#43;%Y%m%d_%H%M%S).log&amp;#34;

echo &amp;#34;Timestamp,CPU%,Memory%,Disk%,Network_RX,Network_TX&amp;#34; &amp;gt; $OUTPUT_FILE

while true; do
    # CPU
    CPU=$(top -bn1 | grep &amp;#34;Cpu(s)&amp;#34; | awk &amp;#39;{print $2}&amp;#39; | cut -d&amp;#39;%&amp;#39; -f1)

    # Memory
    MEM=$(free | awk &amp;#39;NR==2{printf &amp;#34;%.2f&amp;#34;, $3*100/$2}&amp;#39;)

    # Disk (根分区)
    DISK=$(df / | awk &amp;#39;NR==2{print $5}&amp;#39; | cut -d&amp;#39;%&amp;#39; -f1)

    # Network
    NET_RX=$(cat /sys/class/net/eth0/statistics/rx_bytes)
    NET_TX=$(cat /sys/class/net/eth0/statistics/tx_bytes)

    echo &amp;#34;$(date &amp;#43;%s),$CPU,$MEM,$DISK,$NET_RX,$NET_TX&amp;#34; &amp;gt;&amp;gt; $OUTPUT_FILE

    sleep 1
done&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;总结&#34;&gt;总结
&lt;/h2&gt;&lt;p&gt;日志和监控是系统管理的关键技能。通过这篇文章，我们学习了：&lt;/p&gt;
&lt;h3 id=&#34;核心工具&#34;&gt;核心工具
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;journald&lt;/strong&gt;：systemd 日志服务&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;rsyslog&lt;/strong&gt;：传统日志守护进程&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;logrotate&lt;/strong&gt;：日志轮转&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;top/htop&lt;/strong&gt;：进程监控&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;vmstat/iostat&lt;/strong&gt;：性能统计&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;实践技能&#34;&gt;实践技能
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;使用 &lt;code&gt;journalctl&lt;/code&gt; 查询系统日志&lt;/li&gt;
&lt;li&gt;配置日志轮转策略&lt;/li&gt;
&lt;li&gt;分析 CPU、内存、磁盘、网络性能&lt;/li&gt;
&lt;li&gt;编写监控脚本&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;最佳实践&#34;&gt;最佳实践
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;定期检查日志&lt;/strong&gt;：建立日志审查习惯&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;设置告警&lt;/strong&gt;：异常情况及时通知&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;保留历史数据&lt;/strong&gt;：便于趋势分析&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;自动化监控&lt;/strong&gt;：减少人工干预&lt;/li&gt;
&lt;/ol&gt;
&lt;blockquote&gt;
&lt;p&gt;&amp;ldquo;You can&amp;rsquo;t improve what you don&amp;rsquo;t measure.&amp;rdquo; — Peter Drucker&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;监控和日志管理是系统稳定运行的保障。掌握这些工具，让你的 Linux 系统管理更上一层楼！&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;← 返回系列概述&lt;/strong&gt;: &lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/linux-%E6%A6%82%E8%BF%B0%E4%BB%8E-unix-%E5%88%B0%E5%BC%80%E6%BA%90%E9%9D%A9%E5%91%BD/&#34; &gt;Linux 概述：从 Unix 到开源革命&lt;/a&gt;&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;参考资源&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://man7.org/linux/man-pages/man1/journalctl.1.html&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;journalctl 手册&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.rsyslog.com/doc/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;rsyslog 官方文档&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;http://www.brendangregg.com/linuxperf.html&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Linux Performance Tools&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://github.com/sysstat/sysstat&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;sysstat 官方文档&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
        </item>
        <item>
        <title>文件系统：Linux 的数据宫殿</title>
        <link>http://localhost:1313/p/%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9Flinux-%E7%9A%84%E6%95%B0%E6%8D%AE%E5%AE%AB%E6%AE%BF/</link>
        <pubDate>Sun, 11 Jan 2026 01:00:00 +0800</pubDate>
        
        <guid>http://localhost:1313/p/%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9Flinux-%E7%9A%84%E6%95%B0%E6%8D%AE%E5%AE%AB%E6%AE%BF/</guid>
        <description>&lt;img src="http://localhost:1313/default.jpeg" alt="Featured image of post 文件系统：Linux 的数据宫殿" /&gt;&lt;h1 id=&#34;文件系统linux-的数据宫殿&#34;&gt;文件系统：Linux 的数据宫殿
&lt;/h1&gt;&lt;h2 id=&#34;引言&#34;&gt;引言
&lt;/h2&gt;&lt;p&gt;文件系统是操作系统中最重要的组成部分之一。在 Linux 中，一切皆文件——普通文件、目录、设备、套接字等都通过文件系统接口访问。在这篇文章中，我们将深入探讨 Linux 文件系统的奥秘。&lt;/p&gt;
&lt;h2 id=&#34;一切皆文件&#34;&gt;一切皆文件
&lt;/h2&gt;&lt;h3 id=&#34;unix-哲学&#34;&gt;Unix 哲学
&lt;/h3&gt;&lt;p&gt;Linux 继承了 Unix 的设计哲学：&lt;strong&gt;一切皆文件&lt;/strong&gt;&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  mindmap
  root((一切皆文件))
    普通文件
      文本文件
      二进制文件
      数据文件
    目录
      文件夹
      路径
    设备文件
      块设备/dev/sda
      字符设备/dev/tty
    进程文件
      /proc
    网络文件
      /dev/tcp
      Socket
    其他
      管道
      符号链接
&lt;/pre&gt;

&lt;h3 id=&#34;文件类型&#34;&gt;文件类型
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看文件类型
ls -la /etc/passwd /dev/sda /dev/tty /bin/bash

# 输出示例：
# -rw-r--r--  1 root root  2941 Feb  10 10:00 /etc/passwd
# brw-rw----  1 root disk   8, 0 Feb  10 10:00 /dev/sda
# crw-rw-rw-  1 root tty   5, 0 Feb  10 10:00 /dev/tty
# -rwxr-xr-x  1 root root  1.2M Feb  10 10:00 /bin/bash

# 第一个字符表示文件类型：
# - : 普通文件
# d : 目录
# b : 块设备（块设备驱动）
# c : 字符设备（字符设备驱动）
# l : 符号链接
# p : 命名管道（FIFO）
# s : 套接字&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;虚拟文件系统vfs&#34;&gt;虚拟文件系统（VFS）
&lt;/h2&gt;&lt;h3 id=&#34;vfs-的作用&#34;&gt;VFS 的作用
&lt;/h3&gt;&lt;p&gt;VFS 提供了一个统一的文件系统接口，使得 Linux 可以支持多种文件系统：&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart TB
    A[用户程序] --&amp;gt;|open, read, write| B[VFS 层]

    B --&amp;gt; C[Ext4]
    B --&amp;gt; D[XFS]
    B --&amp;gt; E[Btrfs]
    B --&amp;gt; F[NFS]
    B --&amp;gt; G[ProcFS]
    B --&amp;gt; H[SysFS]

    C --&amp;gt; I[块设备层]
    D --&amp;gt; I
    E --&amp;gt; I

    F --&amp;gt; J[网络层]

    G --&amp;gt; K[内核数据结构]
    H --&amp;gt; K

    style B fill:#fff3e0
    style C fill:#e3f2fd
    style D fill:#e3f2fd
    style E fill:#e3f2fd
&lt;/pre&gt;

&lt;h3 id=&#34;vfs-的四大对象&#34;&gt;VFS 的四大对象
&lt;/h3&gt;&lt;h4 id=&#34;1-超级块super_block&#34;&gt;1. 超级块（super_block）
&lt;/h4&gt;&lt;p&gt;超级块表示整个文件系统：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;struct super_block {
    struct list_head s_list;      // 所有超级块的链表
    dev_t s_dev;                  // 设备标识符
    unsigned long s_magic;        // 文件系统魔数

    const struct super_operations *s_op;
    struct file_system_type *s_type;

    struct inode *s_root;         // 根目录 inode
    // ...
};&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;






&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看文件系统超级块信息
tune2fs -l /dev/sda1

# 查看文件系统魔数
blkid /dev/sda1&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;2-索引节点inode&#34;&gt;2. 索引节点（inode）
&lt;/h4&gt;&lt;p&gt;inode 存储文件的元数据（&lt;strong&gt;不包含文件名&lt;/strong&gt;）：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;struct inode {
    umode_t i_mode;               // 文件类型和权限
    uid_t i_uid;                  // 所有者 UID
    gid_t i_gid;                  // 所属组 GID
    loff_t i_size;                // 文件大小（字节）
    struct timespec i_atime;      // 最后访问时间
    struct timespec i_mtime;      // 最后修改时间
    struct timespec i_ctime;      // 最后状态改变时间

    struct inode_operations *i_op;
    struct file_operations *i_fop;

    struct address_space *i_mapping;
    // ...
};&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;inode 包含的信息&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;文件类型&lt;/li&gt;
&lt;li&gt;权限&lt;/li&gt;
&lt;li&gt;所有者&lt;/li&gt;
&lt;li&gt;文件大小&lt;/li&gt;
&lt;li&gt;时间戳&lt;/li&gt;
&lt;li&gt;数据块位置指针&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;inode 不包含&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;文件名（文件名存储在目录项中）&lt;/li&gt;
&lt;/ul&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看 inode 编号
ls -i file.txt

# 查看文件 inode 信息
stat file.txt

# 输出示例：
#   File: file.txt
#   Size: 1234       Blocks: 8         IO Block: 4096   regular file
# Access: (0644/-rw-r--r--)  Uid: ( 1000/   user)   Gid: ( 1000/   user)
# Access: 2026-01-11 00:00:00.000000000 &amp;#43;0800
# Modify: 2026-01-10 23:59:59.000000000 &amp;#43;0800
# Change: 2026-01-10 23:59:59.000000000 &amp;#43;0800
# Birth: 2026-01-10 00:00:00.000000000 &amp;#43;0800&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;3-目录项dentry&#34;&gt;3. 目录项（dentry）
&lt;/h4&gt;&lt;p&gt;dentry 将路径名连接到 inode：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;struct dentry {
    struct dentry *d_parent;      // 父目录
    struct qstr d_name;           // 文件名
    struct inode *d_inode;        // 关联的 inode

    unsigned int d_flags;         // 目录项标志
    struct dentry_operations *d_op;
    // ...
};&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;dentry 缓存（dcache）&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;加速路径查找&lt;/li&gt;
&lt;li&gt;最近使用的目录项保持在内存中&lt;/li&gt;
&lt;/ul&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 清理 dentry 缓存
sync &amp;amp;&amp;amp; echo 3 &amp;gt; /proc/sys/vm/drop_caches&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;4-文件file&#34;&gt;4. 文件（file）
&lt;/h4&gt;&lt;p&gt;file 表示打开的文件：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;struct file {
    struct path f_path;
    struct inode *f_inode;        // 关联的 inode
    const struct file_operations *f_op;

    loff_t f_pos;                 // 当前读写位置
    struct fown_struct f_owner;
    // ...
};&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;






&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看进程打开的文件
lsof -p 1234

# 查看某个文件被哪些进程打开
lsof /var/log/syslog

# /proc 文件系统也提供这些信息
ls -la /proc/1234/fd/&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;文件系统布局&#34;&gt;文件系统布局
&lt;/h2&gt;&lt;h3 id=&#34;ext4-文件系统结构&#34;&gt;Ext4 文件系统结构
&lt;/h3&gt;



  


&lt;div class=&#34;highlight highlight-text&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-text&#34; data-lang=&#34;text&#34;&gt;┌────────────────────────────────────────────────┐
│              Boot Sector (引导扇区)            │
├────────────────────────────────────────────────┤
│              超级块（Superblock）               │
│  - 文件系统大小、块数、inode数                 │
│  - 每1024个块有一个超级块备份                  │
├────────────────────────────────────────────────┤
│              组描述符（Group Descriptors）      │
│  - 块位图、inode位图、inode表的位置            │
├────────────────────────────────────────────────┤
│  数据块组 1  │ 数据块组 2  │ ... │ 数据块组 N  │
│ ┌──────────┐ │ ┌──────────┐ │    │ ┌──────────┐│
│ │超级块备份│ │ │超级块备份│ │    │ │超级块备份││
│ ├──────────┤ │ ├──────────┤ │    │ ├──────────┤│
│ │块位图    │ │ │块位图    │ │    │ │块位图    ││
│ ├──────────┤ │ ├──────────┤ │    │ ├──────────┤│
│ │inode位图 │ │ │inode位图 │ │    │ │inode位图 ││
│ ├──────────┤ │ ├──────────┤ │    │ ├──────────┤│
│ │inode表   │ │ │inode表   │ │    │ │inode表   ││
│ ├──────────┤ │ ├──────────┤ │    │ ├──────────┤│
│ │数据块    │ │ │数据块    │ │    │ │数据块    ││
│ └──────────┘ │ └──────────┘ │    │ └──────────┘│
└────────────────────────────────────────────────┘&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;inode-结构详解&#34;&gt;inode 结构详解
&lt;/h3&gt;&lt;p&gt;inode 包含指向数据块的指针：&lt;/p&gt;




  


&lt;div class=&#34;highlight highlight-text&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-text&#34; data-lang=&#34;text&#34;&gt;┌────────────────────────────────────┐
│         inode 结构                 │
├────────────────────────────────────┤
│  文件元数据（大小、权限、时间等）  │
├────────────────────────────────────┤
│  直接指针 (12个)                   │
│    - 指向数据块 0-11               │
├────────────────────────────────────┤
│  单级间接指针                      │
│    - 指向一个索引块                │
│    - 该索引块包含数据块指针        │
├────────────────────────────────────┤
│  双级间接指针                      │
│    - 指向一个索引块                │
│    - 该索引块指向二级索引块        │
│    - 二级索引块包含数据块指针      │
├────────────────────────────────────┤
│  三级间接指针                      │
│    - 类似双级，但多一层            │
└────────────────────────────────────┘

文件大小计算（假设块大小为 4KB）：
- 直接指针：12 × 4KB = 48KB
- 单级间接：256 × 4KB = 1MB
- 双级间接：256 × 256 × 4KB = 256MB
- 三级间接：256³ × 4KB = 64GB&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;文件权限&#34;&gt;文件权限
&lt;/h2&gt;&lt;h3 id=&#34;权限模型&#34;&gt;权限模型
&lt;/h3&gt;&lt;p&gt;Linux 使用传统的 Unix 权限模型：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;-rw-r--r--  1 user group 1234 Jan 10 10:00 file.txt
││││││││││
│││││││││└─ 其他用户权限（r--）
│││││││└──── 组用户权限（r--）
│││││└─────── 所有者权限（rw-）
│││└───────── 文件类型（- = 普通文件）
│└────────── 特殊权限位
└─────────── ACL 或 SELinux 上下文&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;权限类型&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;r（read）&lt;/strong&gt;：读取&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;w（write）&lt;/strong&gt;：写入&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;x（execute）&lt;/strong&gt;：执行&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;权限对象&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;所有者（user）&lt;/strong&gt;：文件创建者&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;组（group）&lt;/strong&gt;：文件所属组&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;其他（others）&lt;/strong&gt;：其他所有用户&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;权限表示&#34;&gt;权限表示
&lt;/h3&gt;&lt;h4 id=&#34;符号模式&#34;&gt;符号模式
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看权限
ls -l file.txt

# 修改权限
chmod u&amp;#43;x file.txt      # 所有者添加执行权限
chmod go-w file.txt     # 组和其他移除写入权限
chmod a=r file.txt      # 所有用户设置为只读

# 常用组合
chmod 755 script.sh     # rwxr-xr-x
chmod 644 file.txt      # rw-r--r--
chmod 600 secret.key    # rw-------（私密文件）&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;数字模式&#34;&gt;数字模式
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 权限位计算
r = 4
w = 2
x = 1
- = 0

# 示例
rwxr-xr-- = 754
│││││││││
││││││││└─ 其他：4 (r--)
││││││└──── 组：5 (r-x)
││││└─────── 所有者：7 (rwx)

# 常用权限值
644 (rw-r--r--)  # 普通文件
755 (rwxr-xr-x)  # 可执行文件/目录
600 (rw-------)  # 私密文件
640 (rw-r-----)  # 组共享文件&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;目录权限&#34;&gt;目录权限
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 目录权限含义
r (read)    # 可以列出目录内容
w (write)   # 可以创建/删除文件（需要 x 权限）
x (execute) # 可以进入目录（cd）

# 示例
drwxr-xr-x  2 user group 4096 Jan 10 10:00 documents
│││││││││
││││││││└─ 其他：r-x（可以进入，但不能写入）
││││││└──── 组：r-x（可以进入，但不能写入）
││││└─────── 所有者：rwx（完全控制）&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;blockquote class=&#34;alert alert-warning&#34;&gt;
    &lt;p&gt;删除文件需要&lt;strong&gt;父目录的写权限&lt;/strong&gt;，而不是文件本身的权限！&lt;/p&gt;&lt;/blockquote&gt;
&lt;h3 id=&#34;特殊权限&#34;&gt;特殊权限
&lt;/h3&gt;&lt;h4 id=&#34;suidset-user-id&#34;&gt;SUID（Set User ID）
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 以文件所有者身份运行
chmod u&amp;#43;s /usr/bin/passwd
# 或
chmod 4755 /usr/bin/passwd

# 示例：passwd 命令
-rwsr-xr-x  1 root root  68K Jan 10 10:00 /usr/bin/passwd
#           ^
#           s 代替了 x，表示设置了 SUID

# 作用：普通用户运行 passwd 时，临时获得 root 权限
# 从而可以修改 /etc/shadow 文件&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;sgidset-group-id&#34;&gt;SGID（Set Group ID）
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 在目录中创建的文件继承目录的组
chmod g&amp;#43;s /shared/project
# 或
chmod 2755 /shared/project

# 示例
drwxrwsr-x  2 user project 4096 Jan 10 10:00 /shared/project
#           ^
#           s 表示设置了 SGID&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;sticky-bit&#34;&gt;Sticky Bit
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 只有文件所有者才能删除文件
chmod &amp;#43;t /tmp
# 或
chmod 1777 /tmp

# 示例
drwxrwxrwt  10 root root 4096 Jan 10 10:00 /tmp
#            ^
#            t 表示设置了 sticky bit

# 作用：在 /tmp 中，用户只能删除自己的文件&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;默认权限umask&#34;&gt;默认权限（umask）
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看当前 umask
umask
# 输出：0002

# umask 从权限中&amp;#34;减去&amp;#34;这些位
# 文件默认权限：666 - umask = 664 (rw-rw-r--)
# 目录默认权限：777 - umask = 775 (rwxrwxr-x)

# 设置 umask
umask 022              # 新文件权限 644 (rw-r--r--)
umask 077              # 新文件权限 600 (rw-------)

# 永久设置（在 ~/.bashrc 中）
echo &amp;#39;umask 027&amp;#39; &amp;gt;&amp;gt; ~/.bashrc&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;文件操作命令&#34;&gt;文件操作命令
&lt;/h2&gt;&lt;h3 id=&#34;基础操作&#34;&gt;基础操作
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 创建文件
touch file.txt                 # 创建空文件
echo &amp;#34;Hello&amp;#34; &amp;gt; file.txt        # 创建并写入内容
cat &amp;gt; file.txt &amp;lt;&amp;lt; EOF         # 多行输入
Line 1
Line 2
EOF

# 复制文件
cp file1.txt file2.txt         # 复制
cp -r dir1 dir2                # 递归复制目录
cp -p file.txt backup.txt      # 保留属性
cp -i file.txt target.txt      # 覆盖前确认

# 移动/重命名
mv old.txt new.txt             # 重命名
mv file.txt /tmp/              # 移动

# 删除文件
rm file.txt                    # 删除
rm -r dir/                     # 递归删除目录
rm -i file.txt                 # 确认删除
rm -f file.txt                 # 强制删除&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;查找文件&#34;&gt;查找文件
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# find：按条件查找
find /home -name &amp;#34;*.txt&amp;#34;                    # 按名称查找
find /home -type f -name &amp;#34;*.conf&amp;#34;          # 查找文件
find /home -type d -name &amp;#34;log&amp;#34;             # 查找目录
find /home -size &amp;#43;100M                     # 查找大于100MB的文件
find /home -mtime -7                       # 查找7天内修改的文件
find /home -user john                      # 查找john拥有的文件
find /home -perm 777                       # 查找777权限的文件

# locate：快速查找（使用数据库）
locate nginx.conf
sudo updatedb                              # 更新数据库

# which：查找命令位置
which python3
whereis python3                            # 查找二进制、源码、手册页&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;文件内容查看&#34;&gt;文件内容查看
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看文件
cat file.txt                  # 显示全部
less file.txt                 # 分页查看（推荐）
more file.txt                 # 简单分页

# 查看部分
head -n 10 file.txt           # 前10行
tail -n 10 file.txt           # 后10行
tail -f /var/log/syslog       # 实时查看日志

# 查看特定行
sed -n &amp;#39;10,20p&amp;#39; file.txt      # 第10-20行

# 查找内容
grep &amp;#34;error&amp;#34; file.txt
grep -r &amp;#34;TODO&amp;#34; ./             # 递归搜索目录
grep -i &amp;#34;hello&amp;#34; file.txt      # 忽略大小写
grep -n &amp;#34;pattern&amp;#34; file.txt    # 显示行号
grep -v &amp;#34;comment&amp;#34; file.txt    # 反向匹配&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;文件系统操作&#34;&gt;文件系统操作
&lt;/h2&gt;&lt;h3 id=&#34;挂载mount&#34;&gt;挂载（mount）
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看挂载情况
mount                          # 列出所有挂载点
df -h                          # 显示磁盘使用情况

# 挂载文件系统
sudo mount /dev/sdb1 /mnt/data
sudo mount -t ext4 /dev/sdb1 /mnt/data

# 挂载 ISO 镜像
sudo mount -o loop image.iso /mnt

# 挂载 NFS
sudo mount -t nfs server:/share /mnt/nfs

# 卸载
sudo umount /mnt/data

# /etc/fstab：开机自动挂载
# &amp;lt;设备&amp;gt;      &amp;lt;挂载点&amp;gt;   &amp;lt;类型&amp;gt;  &amp;lt;选项&amp;gt;              &amp;lt;dump&amp;gt; &amp;lt;pass&amp;gt;
UUID=xxx    /           ext4    errors=remount-ro   0      1
/dev/sdb1   /mnt/data   ext4    defaults           0      2&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;磁盘空间管理&#34;&gt;磁盘空间管理
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看磁盘使用
df -h                          # 人类可读格式
df -i                          # 查看 inode 使用情况

# 查看目录大小
du -sh /home/user              # 目录总大小
du -h --max-depth=1 /home/user  # 一级子目录大小
du -sh * | sort -hr             # 按大小排序

# 查找大文件
find /home -type f -size &amp;#43;100M -exec ls -lh {} \;

# 清理空间
sudo apt clean                 # 清理 apt 缓存
sudo journalctl --vacuum-time=7d  # 保留7天日志&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;符号链接和硬链接&#34;&gt;符号链接和硬链接
&lt;/h2&gt;&lt;h3 id=&#34;硬链接&#34;&gt;硬链接
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 创建硬链接
ln original.txt link.txt

# 特点：
# 1. 指向同一个 inode
# 2. 同一个文件的不同名字
# 3. 不能跨文件系统
# 4. 不能链接目录
# 5. 删除原文件，链接仍然有效

# 查看
ls -li original.txt link.txt
# 123456 -rw-r--r-- 2 user user ... original.txt
# 123456 -rw-r--r-- 2 user user ... link.txt
#        ↑                  ↑
#      相同 inode        硬链接计数&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;符号链接软链接&#34;&gt;符号链接（软链接）
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 创建符号链接
ln -s /path/to/original symlink

# 特点：
# 1. 指向路径，不是 inode
# 2. 有自己的 inode
# 3. 可以跨文件系统
# 4. 可以链接目录
# 5. 删除原文件，链接失效

# 查看
ls -la symlink
# lrwxrwxrwx 1 user user 13 Jan 10 10:00 symlink -&amp;gt; /path/to/original
#          ↑
#      l 表示符号链接&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;实战技巧&#34;&gt;实战技巧
&lt;/h2&gt;&lt;h3 id=&#34;批量重命名&#34;&gt;批量重命名
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 使用 rename 命令
rename &amp;#39;s/old/new/&amp;#39; *.txt      # 将所有 old 替换为 new
rename &amp;#39;y/a-z/A-Z/&amp;#39; *.txt      # 转换为大写

# 使用 bash 循环
for f in *.jpg; do
    mv &amp;#34;$f&amp;#34; &amp;#34;prefix_${f}&amp;#34;
done&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;查找并删除&#34;&gt;查找并删除
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查找并删除空文件
find . -type f -empty -delete

# 查找并删除旧文件
find /tmp -type f -mtime &amp;#43;30 -delete

# 查找并删除大于100MB的文件
find . -type f -size &amp;#43;100M -exec rm {} \;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;文件备份&#34;&gt;文件备份
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 创建带时间戳的备份
cp file.txt file_$(date &amp;#43;%Y%m%d_%H%M%S).txt

# 使用 rsync 同步
rsync -av --progress /src/ /dst/

# 保留权限、时间戳、所有者
cp -p source.txt backup.txt&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;总结&#34;&gt;总结
&lt;/h2&gt;&lt;p&gt;文件系统是 Linux 的核心概念。通过这篇文章，我们学习了：&lt;/p&gt;
&lt;h3 id=&#34;核心概念&#34;&gt;核心概念
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;一切皆文件&lt;/strong&gt;：Unix 的设计哲学&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;VFS&lt;/strong&gt;：统一多种文件系统的接口&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;inode&lt;/strong&gt;：文件的核心数据结构&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;权限模型&lt;/strong&gt;：用户/组/其他的 rwx 权限&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;文件系统布局&lt;/strong&gt;：超级块、位图、inode表、数据块&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;实践技能&#34;&gt;实践技能
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;使用 &lt;code&gt;chmod&lt;/code&gt;、&lt;code&gt;chown&lt;/code&gt; 管理权限&lt;/li&gt;
&lt;li&gt;理解并设置特殊权限（SUID、SGID、Sticky Bit）&lt;/li&gt;
&lt;li&gt;使用 &lt;code&gt;mount&lt;/code&gt;、&lt;code&gt;umount&lt;/code&gt; 管理文件系统&lt;/li&gt;
&lt;li&gt;创建硬链接和符号链接&lt;/li&gt;
&lt;li&gt;高效查找和管理文件&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;下一步&#34;&gt;下一步
&lt;/h3&gt;&lt;p&gt;在后续文章中，我们将学习：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;设备驱动&lt;/strong&gt;：字符设备和块设备&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;网络编程&lt;/strong&gt;：TCP/IP 和 Socket&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;系统调用&lt;/strong&gt;：深入内核接口&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;&amp;ldquo;In Unix, everything is a file. If it&amp;rsquo;s not a file, it&amp;rsquo;s a process.&amp;rdquo; — Unix Philosophy&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;理解文件系统是理解 Linux 的关键一步。继续探索，你会发现更多精彩！&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;← 返回系列概述&lt;/strong&gt;: &lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/linux-%E6%A6%82%E8%BF%B0%E4%BB%8E-unix-%E5%88%B0%E5%BC%80%E6%BA%90%E9%9D%A9%E5%91%BD/&#34; &gt;Linux 概述：从 Unix 到开源革命&lt;/a&gt;&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;参考资源&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.oreilly.com/library/view/understanding-the-linux/0596005652/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Understanding the Linux Kernel - Chapter 12&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.kernel.org/doc/html/latest/filesystems/vfs.html&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;The Linux VFS&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://ext4.wiki.kernel.org/index.php/Main_Page&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Ext4 Wiki&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://tldp.org/LDP/Linux-Filesystem-Hierarchy/html/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Linux Filesystem Hierarchy&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
        </item>
        <item>
        <title>进程管理：Linux 的生命线</title>
        <link>http://localhost:1313/p/%E8%BF%9B%E7%A8%8B%E7%AE%A1%E7%90%86linux-%E7%9A%84%E7%94%9F%E5%91%BD%E7%BA%BF/</link>
        <pubDate>Sun, 11 Jan 2026 00:00:00 +0800</pubDate>
        
        <guid>http://localhost:1313/p/%E8%BF%9B%E7%A8%8B%E7%AE%A1%E7%90%86linux-%E7%9A%84%E7%94%9F%E5%91%BD%E7%BA%BF/</guid>
        <description>&lt;img src="http://localhost:1313/default.jpeg" alt="Featured image of post 进程管理：Linux 的生命线" /&gt;&lt;h1 id=&#34;进程管理linux-的生命线&#34;&gt;进程管理：Linux 的生命线
&lt;/h1&gt;&lt;h2 id=&#34;引言&#34;&gt;引言
&lt;/h2&gt;&lt;p&gt;进程是 Linux 系统中正在运行的程序的实例。理解进程管理对于系统管理员、开发者和 DevOps 工程师至关重要。在这篇文章中，我们将深入探讨 Linux 如何创建、调度和管理进程。&lt;/p&gt;
&lt;h2 id=&#34;什么是进程&#34;&gt;什么是进程？
&lt;/h2&gt;&lt;h3 id=&#34;进程-vs-程序&#34;&gt;进程 vs 程序
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;程序（Program）&lt;/strong&gt;：存储在磁盘上的可执行文件（静态）&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;进程（Process）&lt;/strong&gt;：正在内存中运行的程序实例（动态）&lt;/li&gt;
&lt;/ul&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart LR
    A[程序&amp;lt;br/&amp;gt;磁盘上的可执行文件] --&amp;gt;|execve| B[进程&amp;lt;br/&amp;gt;内存中的运行实例]
    B --&amp;gt;|fork| C[子进程]
    B --&amp;gt;|exit| D[僵尸进程]

    style A fill:#e3f2fd
    style B fill:#fff3e0
    style C fill:#c8e6c9
    style D fill:#ffcdd2
&lt;/pre&gt;

&lt;h3 id=&#34;进程的特征&#34;&gt;进程的特征
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;独立性&lt;/strong&gt;：每个进程都有独立的地址空间&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;动态性&lt;/strong&gt;：进程是动态创建和销毁的&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;并发性&lt;/strong&gt;：多个进程可以同时运行&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;异步性&lt;/strong&gt;：进程执行速度不可预测&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&#34;进程描述符task_struct&#34;&gt;进程描述符（task_struct）
&lt;/h2&gt;&lt;p&gt;内核使用 &lt;code&gt;task_struct&lt;/code&gt; 结构体来描述进程：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;struct task_struct {
    // 进程状态
    volatile long state;      // 运行状态
    int exit_state;           // 退出状态

    // 进程标识
    pid_t pid;                // 进程ID
    pid_t tgid;               // 线程组ID

    // 进程关系
    struct task_struct *real_parent;   // 真实父进程
    struct task_struct *parent;        // 当前父进程
    struct list_head children;         // 子进程链表

    // 调度相关信息
    struct sched_entity se;            // 调度实体
    int prio;                          // 优先级
    int static_prio;                   // 静态优先级
    int normal_prio;                   // 标准优先级
    unsigned int rt_priority;          // 实时优先级

    // 内存管理
    struct mm_struct *mm;              // 内存描述符
    struct mm_struct *active_mm;

    // 文件系统
    struct fs_struct *fs;              // 文件系统信息
    struct files_struct *files;        // 打开的文件

    // 信号处理
    struct signal_struct *signal;
    struct sighand_struct *sighand;

    // 时间信息
    cputime_t utime, stime;            // 用户态和内核态时间

    // ... 更多字段
};&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;进程标识符&#34;&gt;进程标识符
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看进程 PID
ps -eo pid,cmd | head

# 查看当前 Shell 的 PID
echo $$

# 查看父进程 PID
echo $PPID

# 查看进程组 ID
ps -eo pid,pgid,cmd | head&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;关键标识符&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;PID&lt;/strong&gt;：进程ID（1-32768，循环使用）&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;PPID&lt;/strong&gt;：父进程ID&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;PGID&lt;/strong&gt;：进程组ID&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;SID&lt;/strong&gt;：会话ID&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;进程生命周期&#34;&gt;进程生命周期
&lt;/h2&gt;&lt;h3 id=&#34;进程状态&#34;&gt;进程状态
&lt;/h3&gt;&lt;p&gt;Linux 进程有以下几种状态：&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  stateDiagram-v2
    [*] --&amp;gt; 创建: fork()
    创建 --&amp;gt; 运行: 调度器选中
    运行 --&amp;gt; 可中断睡眠: 等待I/O事件
    运行 --&amp;gt; 不可中断睡眠: 磁盘I/O
    运行 --&amp;gt; 僵尸: exit()
    运行 --&amp;gt; 停止: SIGSTOP
    可中断睡眠 --&amp;gt; 运行: 事件完成
    不可中断睡眠 --&amp;gt; 运行: I/O完成
    停止 --&amp;gt; 运行: SIGCONT
    僵尸 --&amp;gt; [*]: wait()
&lt;/pre&gt;

&lt;table&gt;
  &lt;thead&gt;
      &lt;tr&gt;
          &lt;th&gt;状态&lt;/th&gt;
          &lt;th&gt;内核定义&lt;/th&gt;
          &lt;th&gt;描述&lt;/th&gt;
      &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;运行&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;TASK_RUNNING&lt;/td&gt;
          &lt;td&gt;正在运行或等待运行&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;可中断睡眠&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;TASK_INTERRUPTIBLE&lt;/td&gt;
          &lt;td&gt;等待某些条件，可被信号中断&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;不可中断睡眠&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;TASK_UNINTERRUPTIBLE&lt;/td&gt;
          &lt;td&gt;等待某些条件，不可被中断&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;僵尸&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;TASK_DEAD&lt;/td&gt;
          &lt;td&gt;已终止但父进程未读取退出状态&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;停止&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;TASK_TRACED/STOPPED&lt;/td&gt;
          &lt;td&gt;被调试器暂停或收到 SIGSTOP&lt;/td&gt;
      &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;
&lt;h3 id=&#34;查看进程状态&#34;&gt;查看进程状态
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看所有进程状态
ps aux | head

# STAT 列含义：
# R - Running
# S - Sleeping (interruptible)
# D - Disk sleep (uninterruptible)
# Z - Zombie
# T - Stopped
# &amp;#43; - 前台进程组
# s - 会话leader
# l - 多线程
# N - 低优先级
# &amp;lt; - 高优先级

# 查看特定进程状态
cat /proc/1/status | grep State&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;进程创建&#34;&gt;进程创建
&lt;/h2&gt;&lt;h3 id=&#34;fork-系统调用&#34;&gt;fork() 系统调用
&lt;/h3&gt;&lt;p&gt;&lt;code&gt;fork()&lt;/code&gt; 创建一个几乎与父进程相同的子进程：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;#include &amp;lt;unistd.h&amp;gt;
#include &amp;lt;stdio.h&amp;gt;
#include &amp;lt;sys/types.h&amp;gt;

int main() {
    pid_t pid;

    printf(&amp;#34;Before fork()\n&amp;#34;);

    pid = fork();  // 创建子进程

    if (pid == -1) {
        // fork 失败
        perror(&amp;#34;fork failed&amp;#34;);
        return 1;
    } else if (pid == 0) {
        // 子进程代码
        printf(&amp;#34;Child process: PID = %d, PPID = %d\n&amp;#34;,
               getpid(), getppid());
    } else {
        // 父进程代码
        printf(&amp;#34;Parent process: PID = %d, Child PID = %d\n&amp;#34;,
               getpid(), pid);
    }

    printf(&amp;#34;Common code\n&amp;#34;);
    return 0;
}&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;fork() 的工作原理&lt;/strong&gt;：&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart TD
    A[父进程] --&amp;gt;|fork| B{fork成功?}
    B --&amp;gt;|是| C[创建子进程]
    B --&amp;gt;|否| E[返回-1]
    C --&amp;gt; D[复制父进程资源]
    D --&amp;gt; F[返回子进程PID给父进程]
    D --&amp;gt; G[返回0给子进程]
&lt;/pre&gt;

&lt;h3 id=&#34;写时复制copy-on-write&#34;&gt;写时复制（Copy-on-Write）
&lt;/h3&gt;&lt;p&gt;现代 Linux 使用 COW 优化 fork 性能：&lt;/p&gt;




  


&lt;div class=&#34;highlight highlight-text&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-text&#34; data-lang=&#34;text&#34;&gt;┌─────────────────────────────────────────────────┐
│                  fork() 之前                    │
│  ┌───────────────────────────────────────┐     │
│  │    父进程内存                          │     │
│  │    [代码段 | 数据段 | 堆 | 栈]         │     │
│  └───────────────────────────────────────┘     │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│                  fork() 之后                    │
│  ┌──────────────────────┐  ┌─────────────────┐ │
│  │   父进程             │  │   子进程        │ │
│  │  [共享只读数据]      │  │  [共享只读数据] │ │
│  │  ┌──────────────┐   │  │  ┌───────────┐  │ │
│  │  │ 页表(只读)   │   │  │  │ 页表(只读)│  │ │
│  │  └──────────────┘   │  │  └───────────┘  │ │
│  └──────────────────────┘  └─────────────────┘ │
│                                                     │
│  写入时才复制物理页面                              │
└─────────────────────────────────────────────────┘&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;exec-系列函数&#34;&gt;exec() 系列函数
&lt;/h3&gt;&lt;p&gt;&lt;code&gt;exec()&lt;/code&gt; 用新程序替换当前进程的内存映像：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;#include &amp;lt;unistd.h&amp;gt;
#include &amp;lt;stdio.h&amp;gt;

int main() {
    printf(&amp;#34;Before exec\n&amp;#34;);

    // 执行 ls 命令
    execlp(&amp;#34;ls&amp;#34;, &amp;#34;ls&amp;#34;, &amp;#34;-la&amp;#34;, &amp;#34;/tmp&amp;#34;, NULL);

    // exec 成功则不会执行这里
    perror(&amp;#34;exec failed&amp;#34;);
    return 1;
}&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;exec 函数族&lt;/strong&gt;：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;// 带p的函数在PATH环境变量中搜索程序
execlp(const char *file, const char *arg, ...);
execvp(const char *file, char *const argv[]);

// 不带p的需要完整路径
execl(const char *path, const char *arg, ...);
execv(const char *path, char *const argv[]);

// 带l的函数使用列表传递参数
execl(&amp;#34;/bin/ls&amp;#34;, &amp;#34;ls&amp;#34;, &amp;#34;-la&amp;#34;, NULL);

// 带v的函数使用数组传递参数
char *args[] = {&amp;#34;ls&amp;#34;, &amp;#34;-la&amp;#34;, &amp;#34;/tmp&amp;#34;, NULL};
execv(&amp;#34;/bin/ls&amp;#34;, args);&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;fork--exec-组合&#34;&gt;fork + exec 组合
&lt;/h3&gt;&lt;p&gt;这是 Shell 创建新进程的标准模式：&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  sequenceDiagram
    participant S as Shell
    participant K as Kernel
    participant N as New Program

    S-&amp;gt;&amp;gt;K: fork()
    Note over K: 创建子进程&amp;lt;br/&amp;gt;复制Shell
    K-&amp;gt;&amp;gt;S: 返回子进程PID
    S-&amp;gt;&amp;gt;K: exec(new_program)
    Note over K: 替换子进程内存&amp;lt;br/&amp;gt;加载新程序
    K-&amp;gt;&amp;gt;N: 启动新程序
    N-&amp;gt;&amp;gt;S: 程序结束
    S-&amp;gt;&amp;gt;K: wait() 回收子进程
&lt;/pre&gt;

&lt;h3 id=&#34;wait-和-waitpid&#34;&gt;wait() 和 waitpid()
&lt;/h3&gt;&lt;p&gt;父进程等待子进程结束：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;#include &amp;lt;sys/wait.h&amp;gt;
#include &amp;lt;unistd.h&amp;gt;
#include &amp;lt;stdio.h&amp;gt;
#include &amp;lt;stdlib.h&amp;gt;

int main() {
    pid_t pid;
    int status;

    pid = fork();

    if (pid == 0) {
        // 子进程
        printf(&amp;#34;Child running...\n&amp;#34;);
        sleep(2);
        exit(42);  // 退出码
    } else {
        // 父进程
        printf(&amp;#34;Parent waiting for child...\n&amp;#34;);

        wait(&amp;amp;status);  // 等待任意子进程

        if (WIFEXITED(status)) {
            printf(&amp;#34;Child exited with status: %d\n&amp;#34;,
                   WEXITSTATUS(status));
        }
    }

    return 0;
}&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;等待宏&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;WIFEXITED(status)&lt;/code&gt;：子进程正常退出&lt;/li&gt;
&lt;li&gt;&lt;code&gt;WEXITSTATUS(status)&lt;/code&gt;：获取退出码&lt;/li&gt;
&lt;li&gt;&lt;code&gt;WIFSIGNALED(status)&lt;/code&gt;：子进程被信号终止&lt;/li&gt;
&lt;li&gt;&lt;code&gt;WTERMSIG(status)&lt;/code&gt;：获取终止信号&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;进程调度&#34;&gt;进程调度
&lt;/h2&gt;&lt;h3 id=&#34;cfs完全公平调度器&#34;&gt;CFS（完全公平调度器）
&lt;/h3&gt;&lt;p&gt;CFS 的设计目标：&lt;strong&gt;公平地分配 CPU 时间&lt;/strong&gt;&lt;/p&gt;
&lt;h4 id=&#34;虚拟运行时间vruntime&#34;&gt;虚拟运行时间（vruntime）
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;// vruntime 计算简化版
vruntime &amp;#43;= (delta_exec * NICE_0_LOAD) / load.weight;

// delta_exec: 实际运行时间
// NICE_0_LOAD: nice=0 时的权重（1024）
// load.weight: 进程权重&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart LR
    A[运行1秒] --&amp;gt; B{nice值?}
    B --&amp;gt;|nice=0&amp;lt;br/&amp;gt;weight=1024| C[vruntime += 1s]
    B --&amp;gt;|nice=5&amp;lt;br/&amp;gt;weight=312| D[vruntime += 3.2s]
    B --&amp;gt;|nice=-5&amp;lt;br/&amp;gt;weight=3121| E[vruntime += 0.32s]

    style C fill:#fff3e0
    style D fill:#ffcdd2
    style E fill:#c8e6c9
&lt;/pre&gt;

&lt;h4 id=&#34;红黑树管理可运行进程&#34;&gt;红黑树管理可运行进程
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;// CFS 运行队列
struct cfs_rq {
    struct load_weight load;
    u64 exec_clock;
    u64 min_vruntime;
    struct rb_root tasks_timeline;    // 红黑树根节点
    struct rb_node *rb_leftmost;      // 最左节点（最小vruntime）
    struct task_struct *curr;         // 当前运行进程
};

// 选择下一个运行的进程
static struct sched_entity *pick_next_entity(struct cfs_rq *cfs_rq) {
    struct rb_node *left = cfs_rq-&amp;gt;rb_leftmost;
    return rb_entry(left, struct sched_entity, run_node);
}&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;调度流程&lt;/strong&gt;：&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart TD
    A[时钟中断] --&amp;gt; B[更新当前进程vruntime]
    B --&amp;gt; C[检查是否需要调度]
    C --&amp;gt;|不需要| D[继续运行]
    C --&amp;gt;|需要| E[选择vruntime最小的进程]
    E --&amp;gt; F[切换到新进程]
    F --&amp;gt; G[新进程开始运行]

    style E fill:#fff3e0
&lt;/pre&gt;

&lt;h3 id=&#34;调度策略&#34;&gt;调度策略
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看进程调度策略
ps -eo pid,cmd,policy,rtprio,ni | grep -E &amp;#34;PID|ksoft|kworker&amp;#34;

# POLICY 列含义：
# FF - FIFO 实时调度
# RR - 轮转实时调度
# TS - 分时调度（普通进程）
# IDL - 空闲进程

# 查看实时进程
ps -eo pid,cmd,policy,rtprio | awk &amp;#39;$3 ~ /FF|RR/&amp;#39;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;修改进程优先级&lt;/strong&gt;：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# nice 值：-20（最高优先级）到 19（最低优先级）
# 默认 nice 值为 0

# 以指定 nice 值启动进程
nice -n 10 tar -czf backup.tar.gz /home

# renice：修改运行进程的优先级
renice &amp;#43;5 -p 1234         # 将 PID 1234 的 nice 值改为 &amp;#43;5

# 查看进程 nice 值
ps -eo pid,ni,cmd | head&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;cpu-亲和性&#34;&gt;CPU 亲和性
&lt;/h3&gt;&lt;p&gt;绑定进程到特定 CPU 核心：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看进程 CPU 亲和性
taskset -p 1234           # 显示 PID 1234 的 CPU 亲和性
# pid 1234&amp;#39;s current affinity mask: ff (所有8个CPU)

# 设置进程 CPU 亲和性
taskset -p 3 1234         # 绑定到 CPU 0 和 1（二进制 11）
taskset -pc 0,2,4 1234    # 绑定到 CPU 0, 2, 4

# 启动进程时指定 CPU 亲和性
taskset -c 0,1 ./myapp    # 在 CPU 0 和 1 上运行

# 查看各 CPU 负载
mpstat -P ALL 1&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;进程监控工具&#34;&gt;进程监控工具
&lt;/h2&gt;&lt;h3 id=&#34;ps-命令&#34;&gt;ps 命令
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 基本用法
ps aux                    # BSD 风格
ps -ef                    # System V 风格

# 常用组合
ps aux | grep nginx       # 查找 nginx 进程
ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%mem | head
# 按内存使用排序

# 查看进程树
ps axjf                   # ASCII 艺术进程树
ps -e --forest            # 进程森林

# 查看特定用户进程
ps -u username

# 查看完整命令
ps auxww                  # 不截断命令行&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;tophtop&#34;&gt;top/htop
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# top 命令
top
# 按 P：按 CPU 排序
# 按 M：按内存排序
# 按 T：按时间排序
# 按 k：杀死进程
# 按 r：renice
# 按 q：退出

# htop（更友好）
htop
# F1-F10：功能键
# /：搜索进程
# F9：调整进程优先级
# F6：排序方式&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;pidstat&#34;&gt;pidstat
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 安装 sysstat 包
sudo apt install sysstat

# CPU 使用统计
pidstat 2 5              # 每2秒输出一次，共5次

# 显示特定进程
pidstat -p 1234 1

# 显示线程统计
pidstat -t -p 1234

# 显示上下文切换
pidstat -w -p 1234

# 显示缺页异常
pidstat -r -p 1234&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;proc-文件系统&#34;&gt;/proc 文件系统
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看进程信息
ls /proc/1234/

cat /proc/1234/status     # 进程状态
cat /proc/1234/cmdline    # 命令行参数
cat /proc/1234/environ    # 环境变量
cat /proc/1234/maps       # 内存映射
cat /proc/1234/fd         # 文件描述符
ls -l /proc/1234/fd/      # 查看打开的文件

# 实时监控
watch -n 1 &amp;#39;cat /proc/1234/status | grep -E &amp;#34;VmSize|VmRSS&amp;#34;&amp;#39;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;进程控制&#34;&gt;进程控制
&lt;/h2&gt;&lt;h3 id=&#34;发送信号&#34;&gt;发送信号
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 常用信号
# SIGTERM (15): 优雅终止
# SIGKILL (9): 强制终止
# SIGHUP (1): 重新加载配置
# SIGSTOP (19): 暂停进程
# SIGCONT (18): 继续进程

# kill 命令
kill 1234                # 默认发送 SIGTERM
kill -9 1234             # 强制终止
kill -HUP 1234           # 重新加载

# killall：按名称杀死进程
killall nginx
killall -9 firefox

# pkill：按模式匹配
pkill -f &amp;#34;python.*script&amp;#34;
pkill -u username        # 杀死用户的所有进程

# 查看可用信号
kill -l&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;nohup-和-disown&#34;&gt;nohup 和 disown
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# nohup：忽略 SIGHUP 信号
nohup ./long_process &amp;amp;   # 终端关闭后进程继续运行

# disown：从作业列表中移除
./process &amp;amp;
Ctrl&amp;#43;Z                   # 暂停
bg                       # 后台运行
disown -h %1             # 忽略 SIGHUP

# tmux/screen：更好的会话管理
tmux new -s session      # 创建新会话&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;实战案例&#34;&gt;实战案例
&lt;/h2&gt;&lt;h3 id=&#34;查找并杀死僵尸进程&#34;&gt;查找并杀死僵尸进程
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash
# kill_zombies.sh

# 查找僵尸进程
echo &amp;#34;Checking for zombie processes...&amp;#34;

ps aux | awk &amp;#39;{print $8, $2}&amp;#39; | grep -w Z | while read state pid; do
    if [ -n &amp;#34;$pid&amp;#34; ]; then
        echo &amp;#34;Found zombie process: PID $pid&amp;#34;

        # 查找父进程
        ppid=$(ps -o ppid= -p $pid | tr -d &amp;#39; &amp;#39;)
        echo &amp;#34;Parent PID: $ppid&amp;#34;

        # 通知父进程回收子进程
        kill -CHLD $ppid
        sleep 1

        # 如果仍然存在，杀死父进程
        if ps -p $pid &amp;gt; /dev/null; then
            echo &amp;#34;Zombie still exists, killing parent...&amp;#34;
            kill -9 $ppid
        fi
    fi
done&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;进程监控脚本&#34;&gt;进程监控脚本
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash
# monitor_process.sh

PROCESS_NAME=$1
INTERVAL=5

if [ -z &amp;#34;$PROCESS_NAME&amp;#34; ]; then
    echo &amp;#34;Usage: $0 &amp;lt;process_name&amp;gt;&amp;#34;
    exit 1
fi

while true; do
    clear
    echo &amp;#34;=== Monitoring: $PROCESS_NAME ===&amp;#34;
    echo &amp;#34;Time: $(date)&amp;#34;
    echo &amp;#34;&amp;#34;

    pid=$(pgrep -o &amp;#34;$PROCESS_NAME&amp;#34;)

    if [ -n &amp;#34;$pid&amp;#34; ]; then
        echo &amp;#34;PID: $pid&amp;#34;
        echo &amp;#34;&amp;#34;

        # CPU 和内存使用
        ps -p $pid -o %cpu,%mem,cmd

        echo &amp;#34;&amp;#34;
        echo &amp;#34;--- CPU History ---&amp;#34;
        pidstat -p $pid 1 1 | tail -1

        echo &amp;#34;&amp;#34;
        echo &amp;#34;--- Memory Usage ---&amp;#34;
        grep -E &amp;#34;VmSize|VmRSS&amp;#34; /proc/$pid/status

        echo &amp;#34;&amp;#34;
        echo &amp;#34;--- File Descriptors ---&amp;#34;
        ls /proc/$pid/fd | wc -l
    else
        echo &amp;#34;Process not found!&amp;#34;
    fi

    sleep $INTERVAL
done&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;限制进程资源&#34;&gt;限制进程资源
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# ulimit：查看和设置资源限制
ulimit -a                 # 查看所有限制
ulimit -n 4096            # 设置文件描述符限制
ulimit -u 1024            # 设置进程数限制

# cgroups v2：更强大的资源控制
# 创建 cgroup
sudo mkdir /sys/fs/cgroup/myapp

# 设置内存限制
echo 500M &amp;gt; /sys/fs/cgroup/myapp/memory.max

# 设置 CPU 限制
echo 2 &amp;gt; /sys/fs/cgroup/myapp/cpu.max

# 运行程序
sudo cgexec -g memory,cpu:myapp ./myapp&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;总结&#34;&gt;总结
&lt;/h2&gt;&lt;p&gt;进程管理是 Linux 系统的核心功能。通过这篇文章，我们了解了：&lt;/p&gt;
&lt;h3 id=&#34;核心概念&#34;&gt;核心概念
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;进程 vs 程序&lt;/strong&gt;：静态 vs 动态&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;task_struct&lt;/strong&gt;：内核中的进程描述符&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;进程生命周期&lt;/strong&gt;：从创建到销毁&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;fork/exec/wait&lt;/strong&gt;：进程控制三部曲&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;CFS 调度器&lt;/strong&gt;：公平分配 CPU 时间&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;实践技能&#34;&gt;实践技能
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;使用 &lt;code&gt;ps&lt;/code&gt;、&lt;code&gt;top&lt;/code&gt;、&lt;code&gt;htop&lt;/code&gt; 监控进程&lt;/li&gt;
&lt;li&gt;通过 &lt;code&gt;kill&lt;/code&gt;、&lt;code&gt;killall&lt;/code&gt; 控制进程&lt;/li&gt;
&lt;li&gt;理解进程状态和转换&lt;/li&gt;
&lt;li&gt;分析进程性能问题&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;下一步&#34;&gt;下一步
&lt;/h3&gt;&lt;p&gt;在后续文章中，我们将学习：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;内存管理&lt;/strong&gt;：虚拟内存、分页、交换&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;文件系统&lt;/strong&gt;：VFS、inode、文件权限&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;网络编程&lt;/strong&gt;：Socket、TCP/IP&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;&amp;ldquo;Processes are the living embodiment of running programs.&amp;rdquo; — Michael Kerrisk&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;掌握进程管理是理解 Linux 系统的关键一步。继续深入探索，你会发现更多奥秘！&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;← 返回系列概述&lt;/strong&gt;: &lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/linux-%E6%A6%82%E8%BF%B0%E4%BB%8E-unix-%E5%88%B0%E5%BC%80%E6%BA%90%E9%9D%A9%E5%91%BD/&#34; &gt;Linux 概述：从 Unix 到开源革命&lt;/a&gt;&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;参考资源&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.kernel.org/doc/html/latest/scheduler/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Linux内核进程管理&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.oreilly.com/library/view/understanding-the-linux/0596005652/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Understanding the Linux Kernel - Chapter 3&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://man7.org/tlpi/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;The Linux Programming Interface - Chapter 24&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://man7.org/linux/man-pages/man5/proc.5.html&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;proc(5) - Linux manual page&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
        </item>
        <item>
        <title>Shell 和命令行：Linux 的控制台</title>
        <link>http://localhost:1313/p/shell-%E5%92%8C%E5%91%BD%E4%BB%A4%E8%A1%8Clinux-%E7%9A%84%E6%8E%A7%E5%88%B6%E5%8F%B0/</link>
        <pubDate>Sat, 10 Jan 2026 23:00:00 +0800</pubDate>
        
        <guid>http://localhost:1313/p/shell-%E5%92%8C%E5%91%BD%E4%BB%A4%E8%A1%8Clinux-%E7%9A%84%E6%8E%A7%E5%88%B6%E5%8F%B0/</guid>
        <description>&lt;img src="http://localhost:1313/default.jpeg" alt="Featured image of post Shell 和命令行：Linux 的控制台" /&gt;&lt;h1 id=&#34;shell-和命令行linux-的控制台&#34;&gt;Shell 和命令行：Linux 的控制台
&lt;/h1&gt;&lt;h2 id=&#34;引言&#34;&gt;引言
&lt;/h2&gt;&lt;p&gt;如果说 Linux 内核是汽车的发动机，那么 &lt;strong&gt;Shell&lt;/strong&gt; 就是方向盘和仪表盘。它是用户与操作系统交互的主要界面，是每个 Linux 用户必须掌握的核心技能。在这篇文章中，我们将系统学习 Shell 的方方面面。&lt;/p&gt;
&lt;h2 id=&#34;什么是-shell&#34;&gt;什么是 Shell？
&lt;/h2&gt;&lt;h3 id=&#34;shell-的定义&#34;&gt;Shell 的定义
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;Shell&lt;/strong&gt; 是一个命令解释器，它：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;读取用户输入的命令&lt;/li&gt;
&lt;li&gt;解释并执行命令&lt;/li&gt;
&lt;li&gt;将结果返回给用户&lt;/li&gt;
&lt;/ul&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart LR
    U[用户] --&amp;gt;|输入命令| S[Shell]
    S --&amp;gt;|系统调用| K[内核]
    K --&amp;gt;|执行操作| H[硬件]
    H --&amp;gt;|返回结果| K
    K --&amp;gt;|返回数据| S
    S --&amp;gt;|显示输出| U

    style S fill:#fff3e0
    style K fill:#e3f2fd
&lt;/pre&gt;

&lt;h3 id=&#34;shell-的种类&#34;&gt;Shell 的种类
&lt;/h3&gt;&lt;p&gt;Linux 系统中有多种 Shell：&lt;/p&gt;
&lt;table&gt;
  &lt;thead&gt;
      &lt;tr&gt;
          &lt;th&gt;Shell&lt;/th&gt;
          &lt;th&gt;全称&lt;/th&gt;
          &lt;th&gt;特点&lt;/th&gt;
      &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;sh&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;Bourne Shell&lt;/td&gt;
          &lt;td&gt;最初的 Unix Shell，POSIX 标准&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;bash&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;Bourne Again Shell&lt;/td&gt;
          &lt;td&gt;最常用，sh 的增强版&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;zsh&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;Z Shell&lt;/td&gt;
          &lt;td&gt;功能强大，高度可定制&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;fish&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;Friendly Interactive Shell&lt;/td&gt;
          &lt;td&gt;用户友好，自动补全强大&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;csh/tcsh&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;C Shell&lt;/td&gt;
          &lt;td&gt;语法类似 C 语言&lt;/td&gt;
      &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看系统支持的 Shell
cat /etc/shells

# 查看当前使用的 Shell
echo $SHELL

# 查看 Shell 版本
bash --version&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;为什么选择-bash&#34;&gt;为什么选择 Bash？
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;✅ &lt;strong&gt;默认安装&lt;/strong&gt;：几乎所有 Linux 发行版默认 Shell&lt;/li&gt;
&lt;li&gt;✅ &lt;strong&gt;兼容性好&lt;/strong&gt;：符合 POSIX 标准&lt;/li&gt;
&lt;li&gt;✅ &lt;strong&gt;功能强大&lt;/strong&gt;：命令历史、自动补全、脚本编程&lt;/li&gt;
&lt;li&gt;✅ &lt;strong&gt;社区支持&lt;/strong&gt;：大量教程和脚本资源&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;bash-基础&#34;&gt;Bash 基础
&lt;/h2&gt;&lt;h3 id=&#34;命令的基本结构&#34;&gt;命令的基本结构
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;command [-options] [arguments]
#  命令      选项        参数&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;示例：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;ls -l /home/user
#  ↑  ↑    └─ 参数：操作对象
#  │  └────── 选项：改变命令行为
#  └─────────── 命令：要执行的程序&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;常用命令速查&#34;&gt;常用命令速查
&lt;/h3&gt;&lt;h4 id=&#34;文件和目录操作&#34;&gt;文件和目录操作
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 列出文件
ls              # 简单列出
ls -la          # 详细列表，包括隐藏文件
ls -lh          # 人类可读的文件大小

# 目录切换
cd /home/user   # 绝对路径
cd Documents    # 相对路径
cd ~            # 回到家目录
cd ..           # 返回上级目录
cd -            # 返回上一个目录

# 显示当前目录
pwd

# 创建目录
mkdir dir_name              # 创建单个目录
mkdir -p /a/b/c             # 递归创建多级目录
mkdir -p dir1 dir2 dir3     # 同时创建多个目录

# 删除目录
rmdir empty_dir             # 删除空目录
rm -r dir_name              # 递归删除目录及内容
rm -rf dir_name             # 强制删除，不询问&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;文件操作&#34;&gt;文件操作
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 创建文件
touch file.txt              # 创建空文件
touch file1.txt file2.txt   # 同时创建多个文件

# 复制文件
cp file1.txt file2.txt      # 复制并重命名
cp -r dir1 dir2             # 递归复制目录
cp -p file1.txt file2.txt   # 保留属性

# 移动/重命名
mv old_name new_name        # 重命名
mv file.txt /tmp/           # 移动文件

# 删除文件
rm file.txt                 # 删除文件
rm -i file.txt              # 删除前确认
rm -f file.txt              # 强制删除

# 查看文件内容
cat file.txt                # 显示全部内容
less file.txt               # 分页查看（推荐）
head -n 10 file.txt         # 查看前10行
tail -n 10 file.txt         # 查看后10行
tail -f /var/log/syslog     # 实时查看日志

# 编辑文件
nano file.txt               # 简单编辑器
vim file.txt                # 强大的编辑器&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;文件查找&#34;&gt;文件查找
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# find：按条件查找文件
find /home -name &amp;#34;*.txt&amp;#34;              # 按名称查找
find /home -type f -name &amp;#34;*.conf&amp;#34;     # 查找文件
find /home -type d -name &amp;#34;log&amp;#34;        # 查找目录
find /home -size &amp;#43;100M                # 查找大于100MB的文件
find /home -mtime -7                  # 查找7天内修改的文件

# locate：快速查找（使用数据库）
locate nginx.conf                     # 查找文件
sudo updatedb                         # 更新数据库

# which：查找命令位置
which python3
which ls

# grep：搜索文件内容
grep &amp;#34;error&amp;#34; /var/log/syslog          # 搜索包含error的行
grep -r &amp;#34;TODO&amp;#34; ./                     # 递归搜索目录
grep -i &amp;#34;hello&amp;#34; file.txt              # 忽略大小写
grep -n &amp;#34;pattern&amp;#34; file.txt            # 显示行号
grep -v &amp;#34;comment&amp;#34; file.txt            # 反向匹配&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;环境变量&#34;&gt;环境变量
&lt;/h2&gt;&lt;h3 id=&#34;什么是环境变量&#34;&gt;什么是环境变量
&lt;/h3&gt;&lt;p&gt;环境变量是存储在 Shell 中的键值对，用于控制系统行为和存储配置信息。&lt;/p&gt;
&lt;h3 id=&#34;常见环境变量&#34;&gt;常见环境变量
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;$HOME          # 当前用户家目录
$USER          # 当前用户名
$PATH          # 命令搜索路径
$PWD           # 当前工作目录
$SHELL         # 当前使用的 Shell
$HOSTNAME      # 主机名
$LANG          # 语言和本地化设置
$DISPLAY       # X 显示服务器
$EDITOR        # 默认编辑器&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;环境变量操作&#34;&gt;环境变量操作
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看所有环境变量
env
printenv
export

# 查看特定变量
echo $PATH
echo $HOME

# 设置临时变量（当前 Shell 会话有效）
MY_VAR=&amp;#34;hello&amp;#34;
export MY_VAR                    # 导出为环境变量

# 设置永久变量（写入配置文件）
echo &amp;#39;export MY_VAR=&amp;#34;hello&amp;#34;&amp;#39; &amp;gt;&amp;gt; ~/.bashrc
source ~/.bashrc                 # 重新加载配置

# 删除变量
unset MY_VAR&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;path-变量详解&#34;&gt;PATH 变量详解
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看 PATH
echo $PATH
# 输出：/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

# 添加新路径到 PATH（临时）
export PATH=$PATH:/opt/myapp/bin

# 添加新路径到 PATH（永久）
echo &amp;#39;export PATH=$PATH:/opt/myapp/bin&amp;#39; &amp;gt;&amp;gt; ~/.bashrc

# 命令查找顺序
# 1. 别名（alias）
# 2. 内置命令（builtin）
# 3. PATH 中的可执行文件&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;blockquote class=&#34;alert alert-note&#34;&gt;
    &lt;p&gt;修改 PATH 时注意安全，不要在前面添加不可信的路径，以免被恶意程序替换系统命令。&lt;/p&gt;&lt;/blockquote&gt;
&lt;h2 id=&#34;管道和重定向&#34;&gt;管道和重定向
&lt;/h2&gt;&lt;h3 id=&#34;标准流&#34;&gt;标准流
&lt;/h3&gt;&lt;p&gt;每个进程默认打开三个流：&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart LR
    F[文件] --&amp;gt;|0 stdin| P[进程]
    P --&amp;gt;|1 stdout| T[终端]
    P --&amp;gt;|2 stderr| T

    style F fill:#e3f2fd
    style P fill:#fff3e0
    style T fill:#f3e5f5
&lt;/pre&gt;

&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;stdin（0）&lt;/strong&gt;：标准输入，默认来自键盘&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;stdout（1）&lt;/strong&gt;：标准输出，默认输出到终端&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;stderr（2）&lt;/strong&gt;：标准错误，默认输出到终端&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;输出重定向&#34;&gt;输出重定向
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 覆盖重定向（&amp;gt;）
echo &amp;#34;Hello&amp;#34; &amp;gt; file.txt          # 覆盖文件内容

# 追加重定向（&amp;gt;&amp;gt;）
echo &amp;#34;World&amp;#34; &amp;gt;&amp;gt; file.txt         # 追加到文件末尾

# 重定向标准错误（2&amp;gt;）
command 2&amp;gt; error.log             # 错误输出到文件
command 2&amp;gt;&amp;gt; error.log            # 追加错误

# 同时重定向
command &amp;gt; output.log 2&amp;gt;&amp;amp;1        # 标准输出和错误都重定向
command &amp;amp;&amp;gt; log.txt               # 同上（Bash 简写）
command &amp;gt; /dev/null 2&amp;gt;&amp;amp;1         # 丢弃所有输出

# 只保留错误
command 2&amp;gt;&amp;amp;1 &amp;gt; /dev/null         # 只显示错误&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;输入重定向&#34;&gt;输入重定向
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 从文件读取输入
mysql -u root -p &amp;lt; backup.sql

# Here Document
cat &amp;lt;&amp;lt; EOF
Hello
This is a
multi-line text
EOF

# Here String（Bash 特有）
grep &amp;#34;pattern&amp;#34; &amp;lt;&amp;lt;&amp;lt; &amp;#34;Hello World&amp;#34;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;管道&#34;&gt;管道
&lt;/h3&gt;&lt;p&gt;管道将一个命令的输出作为另一个命令的输入：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;command1 | command2 | command3

# 示例
ps aux | grep nginx | grep -v grep
cat /var/log/syslog | grep &amp;#34;error&amp;#34; | tail -20

# 常用管道组合
ps aux | sort -k4 -nr | head -10      # CPU 使用率最高的进程
du -h /home | sort -hr | head -10     # 占用空间最大的目录
history | awk &amp;#39;{print $2}&amp;#39; | sort | uniq -c | sort -nr | head&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart LR
    A[命令1] --&amp;gt;|stdout| B[管道]
    B --&amp;gt;|stdin| C[命令2]
    C --&amp;gt;|stdout| D[命令3]
    D --&amp;gt;|stdout| T[终端]

    style B fill:#fff3e0
&lt;/pre&gt;

&lt;h2 id=&#34;命令别名和函数&#34;&gt;命令别名和函数
&lt;/h2&gt;&lt;h3 id=&#34;别名alias&#34;&gt;别名（alias）
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 创建别名
alias ll=&amp;#39;ls -laF&amp;#39;
alias grep=&amp;#39;grep --color=auto&amp;#39;
alias c=&amp;#39;clear&amp;#39;

# 查看所有别名
alias

# 删除别名
unalias ll

# 永久保存（写入 ~/.bashrc）
echo &amp;#34;alias ll=&amp;#39;ls -laF&amp;#39;&amp;#34; &amp;gt;&amp;gt; ~/.bashrc&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;常用别名推荐&#34;&gt;常用别名推荐
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 在 ~/.bashrc 中添加
alias ll=&amp;#39;ls -alF&amp;#39;
alias la=&amp;#39;ls -A&amp;#39;
alias l=&amp;#39;ls -CF&amp;#39;
alias ..=&amp;#39;cd ..&amp;#39;
alias ...=&amp;#39;cd ../..&amp;#39;
alias grep=&amp;#39;grep --color=auto&amp;#39;
alias fgrep=&amp;#39;fgrep --color=auto&amp;#39;
alias egrep=&amp;#39;egrep --color=auto&amp;#39;
alias df=&amp;#39;df -h&amp;#39;
alias du=&amp;#39;du -h&amp;#39;
alias free=&amp;#39;free -h&amp;#39;
alias tree=&amp;#39;tree -C&amp;#39;
alias mkdir=&amp;#39;mkdir -pv&amp;#39;
alias wget=&amp;#39;wget -c&amp;#39;
alias histg=&amp;#39;history | grep&amp;#39;
alias myip=&amp;#39;curl http://ipecho.net/plain; echo&amp;#39;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;函数&#34;&gt;函数
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 定义函数
function mkcd() {
    mkdir -p &amp;#34;$1&amp;#34; &amp;amp;&amp;amp; cd &amp;#34;$1&amp;#34;
}

# 或简化写法
mkcd() {
    mkdir -p &amp;#34;$1&amp;#34; &amp;amp;&amp;amp; cd &amp;#34;$1&amp;#34;
}

# 使用函数
mkcd newproject

# 查看函数定义
declare -f mkcd

# 删除函数
unset -f mkcd&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;实用函数示例&#34;&gt;实用函数示例
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 提取压缩文件
extract() {
    if [ -f $1 ]; then
        case $1 in
            *.tar.bz2)   tar xjf $1   ;;
            *.tar.gz)    tar xzf $1   ;;
            *.bz2)       bunzip2 $1   ;;
            *.rar)       unrar x $1   ;;
            *.gz)        gunzip $1    ;;
            *.tar)       tar xf $1    ;;
            *.tbz2)      tar xjf $1   ;;
            *.tgz)       tar xzf $1   ;;
            *.zip)       unzip $1     ;;
            *.Z)         uncompress $1;;
            *.7z)        7z x $1      ;;
            *)     echo &amp;#34;&amp;#39;$1&amp;#39; cannot be extracted via extract()&amp;#34; ;;
        esac
    else
        echo &amp;#34;&amp;#39;$1&amp;#39; is not a valid file&amp;#34;
    fi
}

# 创建备份
backup() {
    tar -czvf &amp;#34;backup_$(date &amp;#43;%Y%m%d_%H%M%S).tar.gz&amp;#34; &amp;#34;$@&amp;#34;
}

# 查找并杀死进程
killport() {
    lsof -ti:$1 | xargs kill -9
}

# 快速搜索文件内容
search() {
    grep -r &amp;#34;$1&amp;#34; . --include=&amp;#34;*.py&amp;#34; --include=&amp;#34;*.js&amp;#34; --include=&amp;#34;*.html&amp;#34;
}&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;bash-脚本编程&#34;&gt;Bash 脚本编程
&lt;/h2&gt;&lt;h3 id=&#34;脚本基础&#34;&gt;脚本基础
&lt;/h3&gt;&lt;h4 id=&#34;第一个脚本&#34;&gt;第一个脚本
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash
# Shebang: 指定解释器

# 这是一个注释

echo &amp;#34;Hello, World!&amp;#34;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;






&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 赋予执行权限
chmod &amp;#43;x script.sh

# 执行脚本
./script.sh

# 或使用 bash 解释
bash script.sh&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;变量&#34;&gt;变量
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash

# 变量定义（注意：等号两边不能有空格）
name=&amp;#34;Zhang San&amp;#34;
age=25

# 变量使用
echo &amp;#34;My name is $name&amp;#34;
echo &amp;#34;I am $age years old&amp;#34;

# 只读变量
readonly PI=3.14159

# 删除变量
unset age

# 特殊变量
echo &amp;#34;Script name: $0&amp;#34;           # 脚本名称
echo &amp;#34;First arg: $1&amp;#34;             # 第一个参数
echo &amp;#34;Second arg: $2&amp;#34;            # 第二个参数
echo &amp;#34;All args: $@&amp;#34;              # 所有参数
echo &amp;#34;Number of args: $#&amp;#34;        # 参数个数
echo &amp;#34;Process ID: $$&amp;#34;            # 当前进程PID
echo &amp;#34;Exit status: $?&amp;#34;           # 上一条命令的退出状态&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;数组&#34;&gt;数组
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash

# 定义数组
fruits=(&amp;#34;Apple&amp;#34; &amp;#34;Banana&amp;#34; &amp;#34;Orange&amp;#34;)

# 访问数组元素
echo ${fruits[0]}      # 第一个元素
echo ${fruits[@]}      # 所有元素
echo ${#fruits[@]}     # 数组长度
echo ${!fruits[@]}     # 所有索引

# 添加元素
fruits&amp;#43;=(&amp;#34;Grape&amp;#34;)

# 遍历数组
for fruit in &amp;#34;${fruits[@]}&amp;#34;; do
    echo &amp;#34;I like $fruit&amp;#34;
done&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;条件判断&#34;&gt;条件判断
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash

# if 语句
age=18

if [ $age -ge 18 ]; then
    echo &amp;#34;You are an adult&amp;#34;
elif [ $age -ge 13 ]; then
    echo &amp;#34;You are a teenager&amp;#34;
else
    echo &amp;#34;You are a child&amp;#34;
fi

# 文件测试
filename=&amp;#34;test.txt&amp;#34;

if [ -f &amp;#34;$filename&amp;#34; ]; then
    echo &amp;#34;File exists&amp;#34;
elif [ -d &amp;#34;$filename&amp;#34; ]; then
    echo &amp;#34;Directory exists&amp;#34;
else
    echo &amp;#34;Not found&amp;#34;
fi

# 字符串比较
str1=&amp;#34;hello&amp;#34;
str2=&amp;#34;world&amp;#34;

if [ &amp;#34;$str1&amp;#34; = &amp;#34;$str2&amp;#34; ]; then
    echo &amp;#34;Strings are equal&amp;#34;
elif [ &amp;#34;$str1&amp;#34; != &amp;#34;$str2&amp;#34; ]; then
    echo &amp;#34;Strings are different&amp;#34;
fi

# 逻辑运算
if [ $age -ge 18 ] &amp;amp;&amp;amp; [ $age -le 65 ]; then
    echo &amp;#34;Working age&amp;#34;
fi&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;常用测试操作符&lt;/strong&gt;：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 文件测试
-f file    # 文件存在且是普通文件
-d dir     # 目录存在
-r file    # 文件可读
-w file    # 文件可写
-x file    # 文件可执行
-s file    # 文件大小非零

# 字符串测试
-z str     # 字符串为空
-n str     # 字符串非空
str1 = str2    # 字符串相等
str1 != str2   # 字符串不等

# 数值比较
num1 -eq num2  # 等于
num1 -ne num2  # 不等于
num1 -gt num2  # 大于
num1 -lt num2  # 小于
num1 -ge num2  # 大于等于
num1 -le num2  # 小于等于&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;循环&#34;&gt;循环
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash

# for 循环
for i in {1..10}; do
    echo &amp;#34;Number: $i&amp;#34;
done

# 遍历文件
for file in *.txt; do
    echo &amp;#34;Processing: $file&amp;#34;
done

# C 风格 for 循环
for ((i=0; i&amp;lt;10; i&amp;#43;&amp;#43;)); do
    echo $i
done

# while 循环
count=0
while [ $count -lt 10 ]; do
    echo &amp;#34;Count: $count&amp;#34;
    ((count&amp;#43;&amp;#43;))
done

# 读取文件行
while IFS= read -r line; do
    echo &amp;#34;$line&amp;#34;
done &amp;lt; input.txt

# until 循环
count=0
until [ $count -ge 10 ]; do
    echo &amp;#34;Count: $count&amp;#34;
    ((count&amp;#43;&amp;#43;))
done&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;函数-1&#34;&gt;函数
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash

# 定义函数
greet() {
    local name=$1
    echo &amp;#34;Hello, $name!&amp;#34;
}

# 调用函数
greet &amp;#34;Zhang San&amp;#34;

# 返回值
add() {
    local num1=$1
    local num2=$2
    return $((num1 &amp;#43; num2))
}

add 5 3
echo &amp;#34;Sum: $?&amp;#34;   # 获取返回值

# 返回字符串（使用 echo）
get_info() {
    echo &amp;#34;Name: $1&amp;#34;
    echo &amp;#34;Age: $2&amp;#34;
}

result=$(get_info &amp;#34;Zhang San&amp;#34; 25)
echo &amp;#34;$result&amp;#34;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;实战示例&#34;&gt;实战示例
&lt;/h3&gt;&lt;h4 id=&#34;系统监控脚本&#34;&gt;系统监控脚本
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash

# system_monitor.sh - 简单系统监控脚本

echo &amp;#34;=== System Monitor ===&amp;#34;
echo &amp;#34;Time: $(date)&amp;#34;
echo &amp;#34;&amp;#34;

# CPU 使用率
echo &amp;#34;--- CPU Usage ---&amp;#34;
top -bn1 | grep &amp;#34;Cpu(s)&amp;#34; | sed &amp;#34;s/.*, *\([0-9.]*\)%* id.*/\1/&amp;#34; \
    | awk &amp;#39;{print &amp;#34;CPU Usage: &amp;#34; 100 - $1&amp;#34;%&amp;#34;}&amp;#39;

# 内存使用
echo &amp;#34;&amp;#34;
echo &amp;#34;--- Memory Usage ---&amp;#34;
free -h | awk &amp;#39;NR==2{printf &amp;#34;Total: %s\nUsed: %s\nFree: %s\nUsage: %.2f%%\n&amp;#34;,
    $2,$3,$4,$3*100/($3&amp;#43;$4)}&amp;#39;

# 磁盘使用
echo &amp;#34;&amp;#34;
echo &amp;#34;--- Disk Usage ---&amp;#34;
df -h | awk &amp;#39;NR&amp;gt;1 &amp;amp;&amp;amp; $5&amp;#43;0 &amp;gt; 80 {printf &amp;#34;⚠️  %s is %s full\n&amp;#34;,$5,$6}&amp;#39;

# 前5个进程
echo &amp;#34;&amp;#34;
echo &amp;#34;--- Top 5 Processes ---&amp;#34;
ps aux --sort=-%cpu | head -6 | awk &amp;#39;{printf &amp;#34;%-10s %-10s %-10s %s\n&amp;#34;,$1,$2,$3,$11}&amp;#39;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;日志备份脚本&#34;&gt;日志备份脚本
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash

# backup_logs.sh - 备份并压缩日志文件

LOG_DIR=&amp;#34;/var/log&amp;#34;
BACKUP_DIR=&amp;#34;/backup/logs&amp;#34;
DATE=$(date &amp;#43;%Y%m%d_%H%M%S)
BACKUP_FILE=&amp;#34;logs_backup_${DATE}.tar.gz&amp;#34;

# 检查并创建备份目录
[ ! -d &amp;#34;$BACKUP_DIR&amp;#34; ] &amp;amp;&amp;amp; mkdir -p &amp;#34;$BACKUP_DIR&amp;#34;

# 查找7天内的日志文件并备份
find &amp;#34;$LOG_DIR&amp;#34; -name &amp;#34;*.log&amp;#34; -mtime -7 -print0 | \
    tar -czvf &amp;#34;${BACKUP_DIR}/${BACKUP_FILE}&amp;#34; --null -T -

if [ $? -eq 0 ]; then
    echo &amp;#34;✅ Backup successful: ${BACKUP_DIR}/${BACKUP_FILE}&amp;#34;

    # 只保留最近30天的备份
    find &amp;#34;$BACKUP_DIR&amp;#34; -name &amp;#34;logs_backup_*.tar.gz&amp;#34; -mtime &amp;#43;30 -delete
    echo &amp;#34;🗑️  Old backups cleaned up&amp;#34;
else
    echo &amp;#34;❌ Backup failed!&amp;#34;
    exit 1
fi&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;命令行技巧&#34;&gt;命令行技巧
&lt;/h2&gt;&lt;h3 id=&#34;命令历史&#34;&gt;命令历史
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看历史
history

# 执行历史中的第 n 条命令
!100

# 执行上一条命令
!!

# 执行上一条命令的参数
!$:   # 上一个命令的最后一个参数
!*:   # 上一个命令的所有参数

# 搜索历史（Ctrl&amp;#43;R 反向搜索）
# (type) Ctrl&amp;#43;R
# (reverse-i-search)`ssh&amp;#39;: ssh user@192.168.1.100

# 清空历史
history -c

# 设置历史大小
echo &amp;#39;HISTSIZE=10000&amp;#39; &amp;gt;&amp;gt; ~/.bashrc
echo &amp;#39;HISTFILESIZE=20000&amp;#39; &amp;gt;&amp;gt; ~/.bashrc&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;命令补全&#34;&gt;命令补全
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Tab 键补全
# 1. 命令补全
# (type) git &amp;lt;Tab&amp;gt;
# 显示：git  git-receive-pack  git-shell  git-upload-pack  git-upload-archive

# 2. 路径补全
# (type) cd /ho&amp;lt;Tab&amp;gt;
# 自动补全为 cd /home/

# 3. 参数补全
# (type) systemctl s&amp;lt;Tab&amp;gt;&amp;lt;Tab&amp;gt;
# 显示可能的参数：start  stop  status  restart

# Bash 补全配置
# 安装 bash-completion
sudo apt install bash-completion

# 启用补全（在 ~/.bashrc 中）
if ! shopt -oq posix; then
  source /etc/bash_completion
fi&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;快捷键&#34;&gt;快捷键
&lt;/h3&gt;&lt;table&gt;
  &lt;thead&gt;
      &lt;tr&gt;
          &lt;th&gt;快捷键&lt;/th&gt;
          &lt;th&gt;功能&lt;/th&gt;
      &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Ctrl+C&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;中断当前命令&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Ctrl+D&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;退出 Shell&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Ctrl+Z&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;暂停当前命令（fg 恢复）&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Ctrl+L&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;清屏&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Ctrl+A&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;移到命令行首&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Ctrl+E&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;移到命令行尾&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Ctrl+U&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;删除到行首&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Ctrl+K&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;删除到行尾&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Ctrl+W&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;删除前一个单词&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Ctrl+Y&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;粘贴删除的内容&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Ctrl+R&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;搜索历史命令&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;!!&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;执行上一条命令&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;!$&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;上一条命令的最后一个参数&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Esc+.&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;上一条命令的最后一个参数（另一种方式）&lt;/td&gt;
      &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;
&lt;h3 id=&#34;作业控制&#34;&gt;作业控制
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 前台和后台
command &amp;amp;           # 后台运行
Ctrl&amp;#43;Z              # 暂停前台命令
bg                  # 将暂停的命令放到后台
fg                  # 将后台命令提到前台

# 查看作业
jobs

# 结束作业
kill %1             # 结束作业号1&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;screentmux-会话管理&#34;&gt;Screen/Tmux 会话管理
&lt;/h3&gt;&lt;p&gt;详见 &lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/post/tmux/&#34; &gt;Tmux 博客文章&lt;/a&gt;，这里简要介绍：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Tmux
tmux new -s session_name       # 创建会话
tmux ls                        # 列出会话
tmux attach -t session_name    # 连接会话
Ctrl&amp;#43;b d                       # 分离会话

# Screen
screen -S session_name         # 创建会话
screen -ls                     # 列出会话
screen -r session_name         # 连接会话
Ctrl&amp;#43;a d                       # 分离会话&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;最佳实践&#34;&gt;最佳实践
&lt;/h2&gt;&lt;h3 id=&#34;1-代码风格&#34;&gt;1. 代码风格
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash
# 脚本描述
# 作者：Your Name
# 日期：YYYY-MM-DD

set -euo pipefail  # 严格模式

# 使用变量而非硬编码
LOG_DIR=&amp;#34;/var/log&amp;#34;
CONFIG_FILE=&amp;#34;/etc/myapp/config.conf&amp;#34;

# 使用引号包裹变量
if [ -f &amp;#34;$CONFIG_FILE&amp;#34; ]; then
    source &amp;#34;$CONFIG_FILE&amp;#34;
fi

# 注释复杂逻辑
# 计算平均值
average=$((total / count))

# 函数命名使用动词
check_status() { :; }
start_service() { :; }
stop_service() { :; }&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;2-错误处理&#34;&gt;2. 错误处理
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash

# 严格模式
set -e   # 遇到错误立即退出
set -u   # 使用未定义变量时报错
set -o pipefail  # 管道中任何命令失败都返回错误

# 或组合使用
set -euo pipefail

# 捕获退出信号
trap &amp;#39;echo &amp;#34;Script interrupted&amp;#34;; exit 1&amp;#39; INT

# 检查命令是否存在
command -v docker &amp;gt;/dev/null 2&amp;gt;&amp;amp;1 || {
    echo &amp;#34;Docker is not installed!&amp;#34;
    exit 1
}

# 检查文件是否存在
[ -f &amp;#34;$CONFIG_FILE&amp;#34; ] || {
    echo &amp;#34;Config file not found!&amp;#34;
    exit 1
}&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;3-日志记录&#34;&gt;3. 日志记录
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash

# 日志函数
log() {
    local level=$1
    shift
    echo &amp;#34;[$(date &amp;#39;&amp;#43;%Y-%m-%d %H:%M:%S&amp;#39;)] [$level] $*&amp;#34; | tee -a app.log
}

log_info() { log &amp;#34;INFO&amp;#34; &amp;#34;$@&amp;#34;; }
log_warn() { log &amp;#34;WARN&amp;#34; &amp;#34;$@&amp;#34;; }
log_error() { log &amp;#34;ERROR&amp;#34; &amp;#34;$@&amp;#34;; }

# 使用
log_info &amp;#34;Starting application...&amp;#34;
log_warn &amp;#34;Configuration file not found, using defaults&amp;#34;
log_error &amp;#34;Failed to connect to database&amp;#34;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;4-参数解析&#34;&gt;4. 参数解析
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;#!/bin/bash

# 显示帮助信息
usage() {
    echo &amp;#34;Usage: $0 [OPTIONS]&amp;#34;
    echo &amp;#34;Options:&amp;#34;
    echo &amp;#34;  -h, --help     Show this help message&amp;#34;
    echo &amp;#34;  -v, --verbose  Verbose mode&amp;#34;
    echo &amp;#34;  -f FILE        Input file&amp;#34;
    exit 0
}

# 解析参数
VERBOSE=false
INPUT_FILE=&amp;#34;&amp;#34;

while [[ $# -gt 0 ]]; do
    case $1 in
        -h|--help)
            usage
            ;;
        -v|--verbose)
            VERBOSE=true
            shift
            ;;
        -f|--file)
            INPUT_FILE=&amp;#34;$2&amp;#34;
            shift 2
            ;;
        *)
            echo &amp;#34;Unknown option: $1&amp;#34;
            usage
            ;;
    esac
done

# 使用参数
if [ &amp;#34;$VERBOSE&amp;#34; = true ]; then
    echo &amp;#34;Verbose mode enabled&amp;#34;
fi

if [ -n &amp;#34;$INPUT_FILE&amp;#34; ]; then
    echo &amp;#34;Processing file: $INPUT_FILE&amp;#34;
fi&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;总结&#34;&gt;总结
&lt;/h2&gt;&lt;p&gt;Shell 和命令行是 Linux 世界的瑞士军刀。掌握 Shell 让你能够：&lt;/p&gt;
&lt;h3 id=&#34;核心能力&#34;&gt;核心能力
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;高效操作&lt;/strong&gt;：命令行比 GUI 快得多&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;自动化&lt;/strong&gt;：脚本自动化重复任务&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;系统管理&lt;/strong&gt;：深入控制和监控&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;远程工作&lt;/strong&gt;：SSH 管理远程服务器&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;学习路径&#34;&gt;学习路径
&lt;/h3&gt;&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart LR
    A[基础命令] --&amp;gt; B[管道重定向]
    B --&amp;gt; C[脚本编程]
    C --&amp;gt; D[高级技巧]
    D --&amp;gt; E[效率提升]

    style A fill:#e8f5e9
    style B fill:#c8e6c9
    style C fill:#a5d6a7
    style D fill:#81c784
    style E fill:#4caf50
&lt;/pre&gt;

&lt;h3 id=&#34;实践建议&#34;&gt;实践建议
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;每日使用&lt;/strong&gt;：让 Shell 成为日常工作的一部分&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;阅读代码&lt;/strong&gt;：学习开源项目的 Shell 脚本&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;编写脚本&lt;/strong&gt;：自动化日常任务&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;探索工具&lt;/strong&gt;：发现新的命令和技巧&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;参考文档&lt;/strong&gt;：&lt;code&gt;man&lt;/code&gt; 和 &lt;code&gt;--help&lt;/code&gt; 是好朋友&lt;/li&gt;
&lt;/ol&gt;
&lt;blockquote&gt;
&lt;p&gt;&amp;ldquo;The shell is the way power users talk to the operating system.&amp;rdquo; — Unix Philosophy&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;在下一篇文章中，我们将深入学习 &lt;strong&gt;进程管理&lt;/strong&gt;，了解 Linux 如何管理和调度进程。&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;← 返回系列概述&lt;/strong&gt;: &lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/linux-%E6%A6%82%E8%BF%B0%E4%BB%8E-unix-%E5%88%B0%E5%BC%80%E6%BA%90%E9%9D%A9%E5%91%BD/&#34; &gt;Linux 概述：从 Unix 到开源革命&lt;/a&gt;&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;参考资源&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.gnu.org/software/bash/manual/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Bash 官方文档&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://tldp.org/LDP/abs/html/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Advanced Bash-Scripting Guide&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.shellcheck.net/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;ShellCheck - Shell 脚本静态分析工具&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://explainshell.com/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;explainshell.com - 命令解释器&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://devhints.io/bash&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;devhints.io/bash&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
        </item>
        <item>
        <title>Linux 内核架构：操作系统的核心</title>
        <link>http://localhost:1313/p/linux-%E5%86%85%E6%A0%B8%E6%9E%B6%E6%9E%84%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E7%9A%84%E6%A0%B8%E5%BF%83/</link>
        <pubDate>Sat, 10 Jan 2026 22:00:00 +0800</pubDate>
        
        <guid>http://localhost:1313/p/linux-%E5%86%85%E6%A0%B8%E6%9E%B6%E6%9E%84%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E7%9A%84%E6%A0%B8%E5%BF%83/</guid>
        <description>&lt;img src="http://localhost:1313/default.jpeg" alt="Featured image of post Linux 内核架构：操作系统的核心" /&gt;&lt;h1 id=&#34;linux-内核架构操作系统的核心&#34;&gt;Linux 内核架构：操作系统的核心
&lt;/h1&gt;&lt;h2 id=&#34;引言&#34;&gt;引言
&lt;/h2&gt;&lt;p&gt;如果说 Linux 操作系统是一辆车，那么**内核（Kernel）**就是这辆车的发动机。它是操作系统的核心，负责管理硬件资源，并为用户程序提供服务。在这篇文章中，我们将深入探讨 Linux 内核的架构和各个子系统。&lt;/p&gt;
&lt;h2 id=&#34;什么是内核&#34;&gt;什么是内核？
&lt;/h2&gt;&lt;h3 id=&#34;内核的定位&#34;&gt;内核的定位
&lt;/h3&gt;&lt;p&gt;内核是操作系统的核心组件，它是硬件和软件之间的桥梁：&lt;/p&gt;




  


&lt;div class=&#34;highlight highlight-text&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-text&#34; data-lang=&#34;text&#34;&gt;┌─────────────────────────────────────────────────┐
│                  应用程序                        │
│         (Browser, Text Editor, Server)          │
├─────────────────────────────────────────────────┤
│              系统调用接口 (SCI)                  │
├─────────────────────────────────────────────────┤
│                 内核空间                         │
│  ┌───────────────────────────────────────────┐  │
│  │  进程管理  │  内存管理  │  VFS  │  网络   │  │
│  └───────────────────────────────────────────┘  │
├─────────────────────────────────────────────────┤
│                  硬件层                          │
│     CPU  │  RAM  │  磁盘  │  网卡  │  设备      │
└─────────────────────────────────────────────────┘&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;内核的主要职责&#34;&gt;内核的主要职责
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;进程管理&lt;/strong&gt;：创建、调度、终止进程&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;内存管理&lt;/strong&gt;：虚拟内存、页面置换、内存分配&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;文件系统&lt;/strong&gt;：文件存储、检索、权限管理&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;设备控制&lt;/strong&gt;：硬件设备驱动和通信&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;网络功能&lt;/strong&gt;：网络协议栈、数据包路由&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&#34;用户空间-vs-内核空间&#34;&gt;用户空间 vs 内核空间
&lt;/h2&gt;&lt;h3 id=&#34;为什么需要分离&#34;&gt;为什么需要分离？
&lt;/h3&gt;&lt;p&gt;现代 CPU 提供了不同的特权级别（Ring 0-3），Linux 使用其中的两个：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;内核空间（Ring 0）&lt;/strong&gt;：最高权限，可直接访问硬件&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;用户空间（Ring 3）&lt;/strong&gt;：受限权限，通过系统调用访问内核&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这种分离带来的好处：&lt;/p&gt;
&lt;p&gt;✅ &lt;strong&gt;安全性&lt;/strong&gt;：防止用户程序直接操作硬件导致系统崩溃
✅ &lt;strong&gt;稳定性&lt;/strong&gt;：进程之间相互隔离，一个崩溃不影响其他进程
✅ &lt;strong&gt;抽象性&lt;/strong&gt;：提供统一的接口，隐藏硬件复杂性&lt;/p&gt;
&lt;h3 id=&#34;地址空间布局&#34;&gt;地址空间布局
&lt;/h3&gt;&lt;p&gt;在 x86-64 架构的 Linux 系统中：&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart TB
    subgraph 用户地址空间 [用户空间 - 128TB]
        A1[代码段&amp;lt;br/&amp;gt;Text Segment]
        A2[数据段&amp;lt;br/&amp;gt;Data Segment]
        A3[堆&amp;lt;br/&amp;gt;Heap - 向上增长]
        A4[内存映射段&amp;lt;br/&amp;gt;Memory Mapping]
        A5[栈&amp;lt;br/&amp;gt;Stack - 向下增长]
    end

    subgraph 内核地址空间 [内核空间 - 128TB]
        B1[内核代码]
        B2[内核数据]
        B3[内核映射]
    end

    A5 -.-&amp;gt;|128TB边界| B1
    A5 -.-&amp;gt;|0x0000...FFFF| B1

    style A1 fill:#e3f2fd
    style A2 fill:#e3f2fd
    style A3 fill:#bbdefb
    style A4 fill:#90caf9
    style A5 fill:#64b5f6
    style B1 fill:#ffccbc
    style B2 fill:#ffab91
    style B3 fill:#ff8a65
&lt;/pre&gt;

&lt;p&gt;&lt;strong&gt;用户空间（0x0000000000000000 - 0x00007FFFFFFFFFFF）&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;每个进程都有独立的用户空间&lt;/li&gt;
&lt;li&gt;通过 &lt;code&gt;malloc()&lt;/code&gt; 分配的内存来自堆&lt;/li&gt;
&lt;li&gt;栈用于函数调用和局部变量&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;内核空间（0xFFFF800000000000 - 0xFFFFFFFFFFFFFFFF）&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;所有进程共享同一个内核空间&lt;/li&gt;
&lt;li&gt;只有内核代码能访问&lt;/li&gt;
&lt;li&gt;用户程序通过系统调用进入内核空间&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;系统调用桥梁&#34;&gt;系统调用：桥梁
&lt;/h3&gt;&lt;p&gt;系统调用是用户程序请求内核服务的唯一合法方式。&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;// 示例：使用 write 系统调用
#include &amp;lt;unistd.h&amp;gt;
#include &amp;lt;string.h&amp;gt;

int main() {
    const char *msg = &amp;#34;Hello, Kernel!\n&amp;#34;;
    // write() 是系统调用的包装函数
    write(STDOUT_FILENO, msg, strlen(msg));
    return 0;
}&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;系统调用流程&lt;/strong&gt;：&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  sequenceDiagram
    participant U as 用户程序
    participant L as 标准库(glibc)
    participant K as 内核

    U-&amp;gt;&amp;gt;L: 调用 write()
    L-&amp;gt;&amp;gt;L: 设置系统调用号
    L-&amp;gt;&amp;gt;K: 软中断(int 0x80/syscall)
    Note over K: 内核态执行
    K-&amp;gt;&amp;gt;K: 检查参数
    K-&amp;gt;&amp;gt;K: 执行写入操作
    K-&amp;gt;&amp;gt;L: 返回结果
    L-&amp;gt;&amp;gt;U: 返回给用户程序
&lt;/pre&gt;

&lt;blockquote class=&#34;alert alert-tip&#34;&gt;
    &lt;p&gt;可以使用 &lt;code&gt;strace&lt;/code&gt; 命令查看程序使用的所有系统调用：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;strace ls /tmp&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;&lt;/blockquote&gt;
&lt;h2 id=&#34;单内核-vs-微内核&#34;&gt;单内核 vs 微内核
&lt;/h2&gt;&lt;h3 id=&#34;单内核monolithic-kernel&#34;&gt;单内核（Monolithic Kernel）
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;Linux 采用单内核架构&lt;/strong&gt;，所有核心服务都在内核空间运行。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;优点&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;✅ &lt;strong&gt;高性能&lt;/strong&gt;：组件间通信开销小&lt;/li&gt;
&lt;li&gt;✅ &lt;strong&gt;简单&lt;/strong&gt;：代码组织直观&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;缺点&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;❌ &lt;strong&gt;稳定性风险&lt;/strong&gt;：一个组件崩溃可能影响整个系统&lt;/li&gt;
&lt;li&gt;❌ &lt;strong&gt;扩展性差&lt;/strong&gt;：添加新功能需要重新编译内核&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;微内核microkernel&#34;&gt;微内核（Microkernel）
&lt;/h3&gt;&lt;p&gt;只保留最基本的机能（进程间通信、调度、低级内存管理）在内核，其他服务作为用户空间进程运行。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;代表系统&lt;/strong&gt;：Minix, QNX, GNU Hurd&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;优点&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;✅ &lt;strong&gt;稳定性高&lt;/strong&gt;：服务崩溃不致命&lt;/li&gt;
&lt;li&gt;✅ &lt;strong&gt;灵活性强&lt;/strong&gt;：可以动态替换服务&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;缺点&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;❌ &lt;strong&gt;性能开销&lt;/strong&gt;：频繁的用户态-内核态切换&lt;/li&gt;
&lt;li&gt;❌ &lt;strong&gt;复杂度高&lt;/strong&gt;：IPC 机制设计复杂&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;linux-的混合方案&#34;&gt;Linux 的混合方案
&lt;/h3&gt;&lt;p&gt;Linux 通过 &lt;strong&gt;可加载内核模块（LKM）&lt;/strong&gt; 在单内核中实现了类似微内核的灵活性：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看已加载的内核模块
lsmod

# 加载模块
sudo modprobe nfs

# 卸载模块
sudo modprobe -r nfs

# 查看模块信息
modinfo nfs&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;内核模块的优势&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;📦 动态加载，无需重启系统&lt;/li&gt;
&lt;li&gt;🔌 扩展内核功能&lt;/li&gt;
&lt;li&gt;🐛 便于调试和开发&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;linux-内核的五大子系统&#34;&gt;Linux 内核的五大子系统
&lt;/h2&gt;&lt;pre class=&#34;mermaid&#34;&gt;
  mindmap
  root((Linux 内核))
    进程调度 SCHED
      CFS调度器
      实时调度
      CPU亲和性
      负载均衡
    内存管理 MM
      虚拟内存
      页表管理
      交换空间
      内存分配器
    虚拟文件系统 VFS
      统一接口
      文件系统抽象
      缓存管理
      dentry/inode
    网络协议栈 NET
      TCP/IP
      Socket接口
      路由和过滤
      Netfilter
    进程间通信 IPC
      管道
      消息队列
      共享内存
      信号量
&lt;/pre&gt;

&lt;h3 id=&#34;1-进程调度sched&#34;&gt;1. 进程调度（SCHED）
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;职责&lt;/strong&gt;：决定哪个进程在什么时候使用 CPU&lt;/p&gt;
&lt;h4 id=&#34;调度器演进&#34;&gt;调度器演进
&lt;/h4&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Linux 2.4&lt;/strong&gt;：O(1) 调度器&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Linux 2.6&lt;/strong&gt;：完全公平调度器（CFS）&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Linux 3.x+&lt;/strong&gt;：改进的 CFS，支持实时任务&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=&#34;cfs完全公平调度器&#34;&gt;CFS（完全公平调度器）
&lt;/h4&gt;&lt;p&gt;CFS 的设计理念：&lt;strong&gt;理想情况下，每个进程应该获得公平的 CPU 时间&lt;/strong&gt;&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;// 伪代码：CFS 调度逻辑
struct cfs_rq {
    struct load_weight load;
    u64 exec_clock;
    u64 min_vruntime;
    struct rb_root tasks_timeline;  // 红黑树
    struct rb_node *rb_leftmost;
};

// 选择下一个运行的进程
struct task_struct *pick_next_task_fair(struct rq *rq) {
    struct cfs_rq *cfs_rq = &amp;amp;rq-&amp;gt;cfs;
    struct rb_node *left = cfs_rq-&amp;gt;rb_leftmost;
    // 返回红黑树最左边的节点（vruntime 最小的任务）
    return rb_entry(left, struct task_struct, run_node);
}&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;关键概念&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;vruntime&lt;/strong&gt;：虚拟运行时间，值越小表示越需要运行&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;nice 值&lt;/strong&gt;：进程优先级，范围 -20（最高）到 19（最低）&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;红黑树&lt;/strong&gt;：高效管理可运行进程&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=&#34;调度策略&#34;&gt;调度策略
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看进程调度策略
ps -eo pid,cmd,policy,rtprio,ni | head -10

# POLICY 列含义：
# FF - FIFO 实时调度
# RR - 轮转实时调度
# TS - 分时调度（普通进程）
# IDL - 空闲进程&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;2-内存管理mm&#34;&gt;2. 内存管理（MM）
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;职责&lt;/strong&gt;：管理虚拟内存、物理内存的映射和分配&lt;/p&gt;
&lt;h4 id=&#34;虚拟内存概念&#34;&gt;虚拟内存概念
&lt;/h4&gt;&lt;p&gt;每个进程都认为自己拥有独立的、连续的地址空间，但实际上：&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart LR
    A[虚拟地址 0x1000] --&amp;gt;|MMU| B[物理地址 0x8000]
    A2[虚拟地址 0x2000] --&amp;gt;|MMU| B2[物理地址 0x9000]
    A3[虚拟地址 0x3000] --&amp;gt;|MMU| B3[未分配&amp;lt;br/&amp;gt;触发缺页中断]

    style A fill:#e3f2fd
    style B fill:#fff3e0
    style A3 fill:#ffebee
&lt;/pre&gt;

&lt;h4 id=&#34;页表结构&#34;&gt;页表结构
&lt;/h4&gt;&lt;p&gt;x86-64 使用四级页表：&lt;/p&gt;




  


&lt;div class=&#34;highlight highlight-text&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-text&#34; data-lang=&#34;text&#34;&gt;虚拟地址: 48 位
┌─────┬─────┬─────┬─────┬─────┬─────────┐
│ PML4│ PDP │ PD  │ PT  │ 页内偏移  │
│ 9bit│ 9bit│ 9bit│ 9bit│   12bit  │
└─────┴─────┴─────┴─────┴─────────┘
   ↓     ↓     ↓     ↓      ↓
  查表  查表  查表  查表   最终物理地址&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;伙伴系统和-slab-分配器&#34;&gt;伙伴系统和 Slab 分配器
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;伙伴系统&lt;/strong&gt;：管理物理页面，避免外部碎片&lt;/p&gt;




  


&lt;div class=&#34;highlight highlight-text&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-text&#34; data-lang=&#34;text&#34;&gt;order 0: 4KB 页面
order 1: 8KB (2个连续4KB页面)
order 2: 16KB (4个连续4KB页面)
...
order 10: 4MB (1024个连续4KB页面)&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Slab 分配器&lt;/strong&gt;：为内核小对象分配内存，减少内部碎片&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;// 内核对象缓存示例
struct kmem_cache *task_struct_cachep;

task_struct_cachep = kmem_cache_create(&amp;#34;task_struct&amp;#34;,
    sizeof(struct task_struct), 0,
    SLAB_HWCACHE_ALIGN|SLAB_PANIC, NULL);

// 从缓存中分配
struct task_struct *tsk = kmem_cache_alloc(task_struct_cachep, GFP_KERNEL);&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;3-虚拟文件系统vfs&#34;&gt;3. 虚拟文件系统（VFS）
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;职责&lt;/strong&gt;：为不同文件系统提供统一接口&lt;/p&gt;
&lt;h4 id=&#34;vfs-架构&#34;&gt;VFS 架构
&lt;/h4&gt;&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart TB
    A[用户程序] --&amp;gt;|open, read, write| B[VFS 层]

    B --&amp;gt; C[Ext4]
    B --&amp;gt; D[XFS]
    B --&amp;gt; E[Btrfs]
    B --&amp;gt; F[NFS]
    B --&amp;gt; G[ProcFS]

    C --&amp;gt; H[块设备层]
    D --&amp;gt; H
    E --&amp;gt; H

    F --&amp;gt; I[网络层]
    G --&amp;gt; J[内核数据结构]

    style B fill:#fff3e0
    style C fill:#e3f2fd
    style D fill:#e3f2fd
    style E fill:#e3f2fd
&lt;/pre&gt;

&lt;h4 id=&#34;四个核心对象&#34;&gt;四个核心对象
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;// 1. super_block：整个文件系统的信息
struct super_block {
    struct list_head s_list;      // 所有超级块链表
    struct file_system_type *s_type;
    const struct super_operations *s_op;
    unsigned long s_magic;        // 文件系统魔数
    // ...
};

// 2. inode：文件的元数据
struct inode {
    umode_t i_mode;               // 文件类型和权限
    uid_t i_uid;
    gid_t i_gid;
    loff_t i_size;                // 文件大小
    struct inode_operations *i_op;
    // ...
};

// 3. dentry：目录项，连接路径名和 inode
struct dentry {
    struct dentry *d_parent;      // 父目录
    struct qstr d_name;           // 文件名
    struct inode *d_inode;        // 关联的 inode
    // ...
};

// 4. file：打开的文件
struct file {
    struct path f_path;
    struct file_operations *f_op;
    loff_t f_pos;                 // 当前读写位置
    // ...
};&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;文件系统类型&#34;&gt;文件系统类型
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看支持的文件系统
cat /proc/filesystems

# 查看已挂载的文件系统
mount | column -t

# 常见文件系统：
# ext4 - 扩展文件系统第4版
# xfs - 高性能日志文件系统
# btrfs - 写时复制(Copy-on-Write)文件系统
# nfs - 网络文件系统
# proc, sysfs - 虚拟文件系统&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;4-网络协议栈net&#34;&gt;4. 网络协议栈（NET）
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;职责&lt;/strong&gt;：实现 TCP/IP 协议栈&lt;/p&gt;
&lt;h4 id=&#34;网络分层模型&#34;&gt;网络分层模型
&lt;/h4&gt;&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart TD
    A[应用层&amp;lt;br/&amp;gt;HTTP/FTP/SSH] --&amp;gt;|Socket API| B[传输层&amp;lt;br/&amp;gt;TCP/UDP]
    B --&amp;gt;|Segment| C[网络层&amp;lt;br/&amp;gt;IP]
    C --&amp;gt;|Packet| D[数据链路层&amp;lt;br/&amp;gt;Ethernet]
    D --&amp;gt;|Frame| E[物理层&amp;lt;br/&amp;gt;网卡驱动]

    style B fill:#e3f2fd
    style C fill:#fff3e0
    style D fill:#f3e5f5
&lt;/pre&gt;

&lt;h4 id=&#34;socket-缓冲区sk_buff&#34;&gt;Socket 缓冲区（sk_buff）
&lt;/h4&gt;&lt;p&gt;网络数据包在内核中的表示：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;struct sk_buff {
    // 这些指针管理数据区域
    char *head, *data;
    unsigned int len, truesize;

    // 协议层特定信息
    union {
        struct tcphdr *th;
        struct udphdr *uh;
        struct icmphdr *icmph;
    } h;

    union {
        struct iphdr *iph;
        struct ipv6hdr *ipv6h;
    } nh;

    union {
        struct ethhdr *eth;
    } mac;

    struct net_device *dev;  // 发送/接收的网卡
    // ...
};&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;数据包接收流程&#34;&gt;数据包接收流程
&lt;/h4&gt;&lt;pre class=&#34;mermaid&#34;&gt;
  sequenceDiagram
    participant N as 网卡
    participant D as 驱动
    participant S as 协议栈
    participant U as 用户程序

    N-&amp;gt;&amp;gt;D: 硬件中断
    D-&amp;gt;&amp;gt;D: 分配 sk_buff
    D-&amp;gt;&amp;gt;S: netif_rx()
    Note over S: 软中断下半部&amp;lt;br/&amp;gt;处理数据包
    S-&amp;gt;&amp;gt;S: IP 层处理
    S-&amp;gt;&amp;gt;S: TCP/UDP 层处理
    S-&amp;gt;&amp;gt;U: 放入接收队列
    U-&amp;gt;&amp;gt;U: read() 读取数据
&lt;/pre&gt;

&lt;h3 id=&#34;5-进程间通信ipc&#34;&gt;5. 进程间通信（IPC）
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;职责&lt;/strong&gt;：提供进程间数据交换和同步机制&lt;/p&gt;
&lt;h4 id=&#34;ipc-机制对比&#34;&gt;IPC 机制对比
&lt;/h4&gt;&lt;table&gt;
  &lt;thead&gt;
      &lt;tr&gt;
          &lt;th&gt;机制&lt;/th&gt;
          &lt;th&gt;特点&lt;/th&gt;
          &lt;th&gt;速度&lt;/th&gt;
          &lt;th&gt;复杂度&lt;/th&gt;
          &lt;th&gt;适用场景&lt;/th&gt;
      &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;管道&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;单向数据流&lt;/td&gt;
          &lt;td&gt;中&lt;/td&gt;
          &lt;td&gt;低&lt;/td&gt;
          &lt;td&gt;简单父子进程通信&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;命名管道&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;无亲缘关系进程&lt;/td&gt;
          &lt;td&gt;中&lt;/td&gt;
          &lt;td&gt;低&lt;/td&gt;
          &lt;td&gt;进程间简单通信&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;消息队列&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;带类型的数据&lt;/td&gt;
          &lt;td&gt;中&lt;/td&gt;
          &lt;td&gt;中&lt;/td&gt;
          &lt;td&gt;需要格式化的消息&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;共享内存&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;直接内存访问&lt;/td&gt;
          &lt;td&gt;快&lt;/td&gt;
          &lt;td&gt;高&lt;/td&gt;
          &lt;td&gt;大数据量交换&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;信号量&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;同步原语&lt;/td&gt;
          &lt;td&gt;-&lt;/td&gt;
          &lt;td&gt;高&lt;/td&gt;
          &lt;td&gt;进程同步&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;strong&gt;Socket&lt;/strong&gt;&lt;/td&gt;
          &lt;td&gt;网络通信&lt;/td&gt;
          &lt;td&gt;慢&lt;/td&gt;
          &lt;td&gt;高&lt;/td&gt;
          &lt;td&gt;跨主机通信&lt;/td&gt;
      &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;
&lt;h4 id=&#34;管道示例&#34;&gt;管道示例
&lt;/h4&gt;




&lt;div class=&#34;highlight highlight-c&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-c&#34; data-lang=&#34;c&#34;&gt;#include &amp;lt;unistd.h&amp;gt;
#include &amp;lt;stdio.h&amp;gt;

int main() {
    int pipefd[2];
    pid_t pid;
    char buf[32];

    // 创建管道
    pipe(pipefd);

    pid = fork();
    if (pid == 0) {
        // 子进程：写入数据
        write(pipefd[1], &amp;#34;Hello from child&amp;#34;, 16);
        close(pipefd[1]);
    } else {
        // 父进程：读取数据
        read(pipefd[0], buf, 32);
        printf(&amp;#34;Parent received: %s\n&amp;#34;, buf);
        close(pipefd[0]);
    }
    return 0;
}&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;内核源代码结构&#34;&gt;内核源代码结构
&lt;/h2&gt;&lt;h3 id=&#34;获取源代码&#34;&gt;获取源代码
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 从 kernel.org 下载
wget https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.6.1.tar.xz
tar -xf linux-6.6.1.tar.xz
cd linux-6.6.1

# 或使用 git
git clone https://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux.git
cd linux&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;目录结构&#34;&gt;目录结构
&lt;/h3&gt;



  


&lt;div class=&#34;highlight highlight-text&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-text&#34; data-lang=&#34;text&#34;&gt;linux/
├── arch/           # 特定 CPU 架构代码（x86, ARM, RISC-V 等）
├── block/          # 块设备层（磁盘 I/O 调度）
├── crypto/         # 加密算法
├── Documentation/  # 内核文档
├── drivers/        # 设备驱动程序
│   ├── net/        # 网络设备驱动
│   ├── gpu/        # GPU 驱动
│   └── char/       # 字符设备驱动
├── fs/             # 文件系统（ext4, xfs, nfs 等）
├── include/        # 头文件
├── init/           # 内核初始化代码
├── ipc/            # 进程间通信
├── kernel/         # 核心子系统（调度器、信号、能力）
├── lib/            # 通用库函数
├── mm/             # 内存管理
├── net/            # 网络协议栈
├── scripts/        # 构建和配置脚本
├── security/       # 安全框架（SELinux, AppArmor）
├── sound/          # 声音子系统（ALSA）
└── virt/           # 虚拟化支持（KVM）&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;配置和编译&#34;&gt;配置和编译
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 安装依赖
sudo apt install build-essential libncurses-dev bison flex libssl-dev

# 配置内核
make menuconfig    # 文本界面配置
# 或
make defconfig     # 使用默认配置

# 编译（根据 CPU 核心数并行编译）
make -j$(nproc)

# 安装模块
sudo make modules_install

# 安装内核
sudo make install&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;实用工具和命令&#34;&gt;实用工具和命令
&lt;/h2&gt;&lt;h3 id=&#34;内核信息查看&#34;&gt;内核信息查看
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 查看内核版本
uname -r

# 查看内核参数
cat /proc/cmdline

# 查看内核统计信息
cat /proc/stat

# 查看内存信息
cat /proc/meminfo

# 查看中断信息
cat /proc/interrupts

# 动态查看内核日志
dmesg | tail -f&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;性能分析工具&#34;&gt;性能分析工具
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 系统调用追踪
strace -c ls           # 统计系统调用次数
strace -e trace=open,read,write ls  # 追踪特定系统调用

# 内核活动追踪
perf top              # 实时性能分析
perf record -g ./myapp    # 记录性能数据
perf report               # 查看报告

# 网络统计
ss -s                 # 套接字统计
netstat -s            # 网络协议统计

# 进程监控
top                   # 实时进程信息
htop                  # 增强版 top&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;总结&#34;&gt;总结
&lt;/h2&gt;&lt;p&gt;Linux 内核是现代技术世界中最复杂、最重要的软件项目之一。通过这篇文章，我们了解了：&lt;/p&gt;
&lt;h3 id=&#34;核心概念&#34;&gt;核心概念
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;用户空间和内核空间&lt;/strong&gt;：隔离和保护&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;系统调用&lt;/strong&gt;：用户程序和内核之间的桥梁&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;单内核 + 模块化&lt;/strong&gt;：性能和灵活性的平衡&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;五大子系统&lt;/strong&gt;：进程、内存、VFS、网络、IPC&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;内核设计哲学&#34;&gt;内核设计哲学
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;KISS 原则&lt;/strong&gt;：Keep It Simple, Stupid&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;性能优先&lt;/strong&gt;：关键路径优化&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;可移植性&lt;/strong&gt;：支持多种硬件架构&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;开源协作&lt;/strong&gt;：全球开发者共同维护&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;下一步&#34;&gt;下一步
&lt;/h3&gt;&lt;p&gt;在后续的文章中，我们将深入探讨：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;进程管理&lt;/strong&gt;：CFS 调度器、进程生命周期&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;内存管理&lt;/strong&gt;：分页机制、内存分配器&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;文件系统&lt;/strong&gt;：Ext4 原理、VFS 实现&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;网络栈&lt;/strong&gt;：TCP/IP、Socket 编程&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;&amp;ldquo;Read the fucking source code.&amp;rdquo; — Linus Torvalds&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;理解内核最好的方式就是阅读源代码。推荐从 &lt;code&gt;kernel/&lt;/code&gt; 目录开始，逐步深入各个子系统。&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;← 返回系列概述&lt;/strong&gt;: &lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/linux-%E6%A6%82%E8%BF%B0%E4%BB%8E-unix-%E5%88%B0%E5%BC%80%E6%BA%90%E9%9D%A9%E5%91%BD/&#34; &gt;Linux 概述：从 Unix 到开源革命&lt;/a&gt;&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;参考资源&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.kernel.org/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Linux 内核官网&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.kernel.org/doc/html/latest/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Linux 内核文档&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.oreilly.com/library/view/understanding-the-linux/0596005652/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Understanding the Linux Kernel&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.oreilly.com/library/view/linux-kernel-development/0672327201/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Linux Kernel Development&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://medium.com/@ankur_kothari/linux-kernel-architecture-d831cf0135b5&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Linux Kernel Architecture 图解&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
        </item>
        <item>
        <title>Linux 概述：从 Unix 到开源革命</title>
        <link>http://localhost:1313/p/linux-%E6%A6%82%E8%BF%B0%E4%BB%8E-unix-%E5%88%B0%E5%BC%80%E6%BA%90%E9%9D%A9%E5%91%BD/</link>
        <pubDate>Sat, 10 Jan 2026 21:00:48 +0800</pubDate>
        
        <guid>http://localhost:1313/p/linux-%E6%A6%82%E8%BF%B0%E4%BB%8E-unix-%E5%88%B0%E5%BC%80%E6%BA%90%E9%9D%A9%E5%91%BD/</guid>
        <description>&lt;img src="http://localhost:1313/default.jpeg" alt="Featured image of post Linux 概述：从 Unix 到开源革命" /&gt;&lt;h1 id=&#34;linux-概述从-unix-到开源革命&#34;&gt;Linux 概述：从 Unix 到开源革命
&lt;/h1&gt;&lt;h2 id=&#34;引言&#34;&gt;引言
&lt;/h2&gt;&lt;p&gt;Linux 是现代技术世界中最重要的操作系统之一。从你手中的 Android 手机到运行互联网的服务器，从超级计算机到嵌入式设备，Linux 无处不在。这篇文章将带你深入了解 Linux 的前世今生。&lt;/p&gt;
&lt;h2 id=&#34;unix-的诞生1969-1970s&#34;&gt;Unix 的诞生（1969-1970s）
&lt;/h2&gt;&lt;h3 id=&#34;背景&#34;&gt;背景
&lt;/h3&gt;&lt;p&gt;在 1969 年，AT&amp;amp;T 贝尔实验室的 Ken Thompson、Dennis Ritchie 和其他研究人员开发出了一个名为 &lt;strong&gt;Unix&lt;/strong&gt; 的操作系统。这个系统后来成为了现代操作系统的基石。&lt;/p&gt;
&lt;h3 id=&#34;unix-的核心设计哲学&#34;&gt;Unix 的核心设计哲学
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;&amp;ldquo;一切皆文件&amp;rdquo;&lt;/strong&gt;：文件、设备、进程间通信等都以文件形式表示&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;小而精的工具&lt;/strong&gt;：每个程序只做一件事，并且做到极致&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;文本流&lt;/strong&gt;：使用文本作为程序间的通用接口&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;可移植性&lt;/strong&gt;：用 C 语言重写内核，实现了跨平台能力&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;unix-的影响&#34;&gt;Unix 的影响
&lt;/h3&gt;&lt;pre class=&#34;mermaid&#34;&gt;
  timeline
    title Unix/Linux 发展简史
    1969 : Unix 诞生于贝尔实验室
    1971 : Unix 用 C 语言重写
    1973 : Unix 开始商业化
    1983 : Richard Stallman 发起 GNU 项目
    1984 : AT&amp;amp;T 发布 System V
    1987 : Andrew Tanenbaum 开发 Minix
    1991 : Linus Torvalds 发布 Linux 0.01
    1992 : GNU GPL 许可证发布
    1998 : IBM、Oracle 等支持 Linux
    2000s : 服务器市场 Linux 成为主流
    2008 : Android 基于 Linux 发布
    2020s : Linux 统治云计算和容器时代
&lt;/pre&gt;

&lt;h2 id=&#34;gnu-计划和自由软件运动1983&#34;&gt;GNU 计划和自由软件运动（1983）
&lt;/h2&gt;&lt;h3 id=&#34;richard-stallman-的愿景&#34;&gt;Richard Stallman 的愿景
&lt;/h3&gt;&lt;p&gt;1983 年，Richard Stallman（RMS）发起了 &lt;strong&gt;GNU（GNU&amp;rsquo;s Not Unix）&lt;/strong&gt; 项目，目标是创建一个完全自由的 Unix-like 操作系统。&lt;/p&gt;
&lt;h3 id=&#34;四大自由&#34;&gt;四大自由
&lt;/h3&gt;&lt;p&gt;自由软件运动定义了软件使用者的四大基本自由：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;自由度 0&lt;/strong&gt;：出于任何目的运行程序的自由&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;自由度 1&lt;/strong&gt;：研究并修改程序的自由&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;自由度 2&lt;/strong&gt;：重新分发副本的自由&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;自由度 3&lt;/strong&gt;：改进程序并向公众发布的自由&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;copyleft-和-gpl&#34;&gt;Copyleft 和 GPL
&lt;/h3&gt;&lt;p&gt;Stallman 创建了 &lt;strong&gt;GPL（GNU General Public License）&lt;/strong&gt; 许可证，引入了 &lt;strong&gt;Copyleft&lt;/strong&gt; 概念：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Copyright&lt;/strong&gt;：保留所有权利&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Copyleft&lt;/strong&gt;：保留所有权利，但要求衍生作品也以相同条款发布&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这确保了自由软件始终保持自由。&lt;/p&gt;
&lt;h2 id=&#34;linus-torvalds-和-linux-的诞生1991&#34;&gt;Linus Torvalds 和 Linux 的诞生（1991）
&lt;/h2&gt;&lt;h3 id=&#34;背景故事&#34;&gt;背景故事
&lt;/h3&gt;&lt;p&gt;1991 年，芬兰赫尔辛基大学的学生 &lt;strong&gt;Linus Torvalds&lt;/strong&gt; 对当时的教学操作系统 Minix 不满意。他想要一个能在他的 80386 计算机上运行的 Unix-like 系统。&lt;/p&gt;
&lt;h3 id=&#34;历史性的-usenet-帖子&#34;&gt;历史性的 Usenet 帖子
&lt;/h3&gt;&lt;p&gt;1991 年 8 月 25 日，Linus 在 comp.os.minix 新闻组发布了这个著名的帖子：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&amp;ldquo;我正在做一个（免费的）操作系统（只是个爱好，不会像 GNU 那样大而专业）&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;I&amp;rsquo;m doing a (free) operating system (just a hobby, won&amp;rsquo;t be big and professional like gnu)&amp;rdquo;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;事实证明，他严重低估了自己的项目。😄&lt;/p&gt;
&lt;h3 id=&#34;linux-001-版本&#34;&gt;Linux 0.01 版本
&lt;/h3&gt;&lt;p&gt;1991 年 9 月，Linux 0.01 版本发布，包含约 10,000 行代码。虽然功能有限，但它可以运行 bash shell 和 gcc 编译器。&lt;/p&gt;
&lt;pre class=&#34;mermaid&#34;&gt;
  mindmap
  root((Linux 生态系统))
    内核
      Linus Torvalds维护
      开源社区贡献
      硬件抽象层
    GNU工具
      GCC编译器
      Bash shell
      Coreutils
      C库(glibc)
    桌面环境
      GNOME
      KDE
      XFCE
    发行版
      Debian系
      RedHat系
      Arch系
      独立发行版
&lt;/pre&gt;

&lt;h2 id=&#34;linux-vs-gnulinux&#34;&gt;Linux vs GNU/Linux
&lt;/h2&gt;&lt;h3 id=&#34;名称争议&#34;&gt;名称争议
&lt;/h3&gt;&lt;p&gt;严格来说，&lt;strong&gt;Linux&lt;/strong&gt; 只是内核，而完整的操作系统应该称为 &lt;strong&gt;GNU/Linux&lt;/strong&gt;，因为：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Linus 创造了 Linux &lt;strong&gt;内核&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;GNU 项目提供了大部分用户空间工具&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;但在实际使用中，人们普遍使用 &amp;ldquo;Linux&amp;rdquo; 来指代整个操作系统。这也是可以理解的，因为 &amp;ldquo;GNU/Linux&amp;rdquo; 读起来确实比较拗口。&lt;/p&gt;
&lt;h3 id=&#34;各司其职&#34;&gt;各司其职
&lt;/h3&gt;



  


&lt;div class=&#34;highlight highlight-text&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-text&#34; data-lang=&#34;text&#34;&gt;┌─────────────────────────────────────┐
│         应用程序层                   │
│    (Firefox, Vim, Git, etc.)        │
├─────────────────────────────────────┤
│         用户空间                     │
│   (GNU工具, Desktop Environment)    │
├─────────────────────────────────────┤
│      Linux 内核 (系统调用接口)       │
├─────────────────────────────────────┤
│            硬件                      │
└─────────────────────────────────────┘&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;linux-内核的特点&#34;&gt;Linux 内核的特点
&lt;/h2&gt;&lt;h3 id=&#34;单内核架构monolithic-kernel&#34;&gt;单内核架构（Monolithic Kernel）
&lt;/h3&gt;&lt;p&gt;Linux 采用单内核架构，整个内核运行在一个单一的地址空间中：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;优点&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;✅ 高性能：系统调用和模块间通信开销小&lt;/li&gt;
&lt;li&gt;✅ 简单直观：代码组织清晰&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;缺点&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;❌ 稳定性风险：一个模块崩溃可能导致整个系统崩溃&lt;/li&gt;
&lt;li&gt;❌ 难以扩展：添加新功能需要重新编译内核&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;但是！&lt;/strong&gt; Linux 通过 &lt;strong&gt;可加载内核模块（LKM）&lt;/strong&gt; 机制在保持单内核性能优势的同时，获得了微内核的灵活性。&lt;/p&gt;
&lt;h3 id=&#34;内核子系统&#34;&gt;内核子系统
&lt;/h3&gt;&lt;p&gt;Linux 内核包含以下主要子系统：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;进程调度（SCHED）&lt;/strong&gt;：管理 CPU 时间片分配&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;内存管理（MM）&lt;/strong&gt;：虚拟内存、分页、交换&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;虚拟文件系统（VFS）&lt;/strong&gt;：统一的文件系统接口&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;网络协议栈（NET）&lt;/strong&gt;：TCP/IP 网络协议实现&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;进程间通信（IPC）&lt;/strong&gt;：管道、消息队列、共享内存等&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&#34;linux-发行版生态&#34;&gt;Linux 发行版生态
&lt;/h2&gt;&lt;p&gt;Linux 内核 + GNU 工具 + 桌面环境 + 软件包管理器 = &lt;strong&gt;Linux 发行版&lt;/strong&gt;&lt;/p&gt;
&lt;h3 id=&#34;debian-系&#34;&gt;Debian 系
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;特点&lt;/strong&gt;：稳定、社区驱动、APT 包管理器&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Debian&lt;/strong&gt;：最古老的社区发行版之一，以稳定性著称&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Ubuntu&lt;/strong&gt;：基于 Debian，用户友好，适合新手和桌面使用&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Linux Mint&lt;/strong&gt;：基于 Ubuntu，提供传统桌面体验&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Kali Linux&lt;/strong&gt;：专为渗透测试和安全审计设计&lt;/li&gt;
&lt;/ul&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Debian/Ubuntu 包管理示例
sudo apt update           # 更新软件包列表
sudo apt upgrade          # 升级已安装的包
sudo apt install nginx    # 安装软件
sudo apt remove nginx     # 删除软件&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;red-hat-系&#34;&gt;Red Hat 系
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;特点&lt;/strong&gt;：企业级、RPM 包管理器、商业支持&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Red Hat Enterprise Linux (RHEL)&lt;/strong&gt;：企业级 Linux，提供商业支持&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Fedora&lt;/strong&gt;：RHEL 的上游，新技术试验场&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;CentOS&lt;/strong&gt;：RHEL 的免费重建版（已被 Stream 替代）&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Oracle Linux&lt;/strong&gt;：Oracle 的 RHEL 兼容发行版&lt;/li&gt;
&lt;/ul&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# RHEL/Fedora 包管理示例
sudo dnf update           # 更新软件包
sudo dnf install nginx    # 安装软件
sudo dnf remove nginx     # 删除软件&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;arch-系&#34;&gt;Arch 系
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;特点&lt;/strong&gt;：滚动更新、KISS 哲学、文档优秀&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Arch Linux&lt;/strong&gt;：轻量、灵活、滚动更新&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Manjaro&lt;/strong&gt;：基于 Arch，更友好的安装过程&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;EndeavourOS&lt;/strong&gt;：Arch 的另一个友好分支&lt;/li&gt;
&lt;/ul&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Arch 包管理示例
sudo pacman -Syu          # 同步并升级系统
sudo pacman -S nginx      # 安装软件
sudo pacman -R nginx      # 删除软件&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;独立发行版&#34;&gt;独立发行版
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Gentoo&lt;/strong&gt;：源码发行版，高度可定制&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;openSUSE&lt;/strong&gt;：SUSE 公司的社区发行版&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Slackware&lt;/strong&gt;：最古老的存活发行版，保持 Unix 传统&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;选择合适的发行版&#34;&gt;选择合适的发行版
&lt;/h2&gt;&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart TD
    A[选择 Linux 发行版] --&amp;gt; B{使用目的?}

    B --&amp;gt;|服务器| C{稳定性 vs 新特性}
    B --&amp;gt;|桌面日常| D{新手 vs 老手}
    B --&amp;gt;|学习原理| E[Arch Linux]
    B --&amp;gt;|渗透测试| F[Kali Linux]

    C --&amp;gt;|企业生产| G[RHEL/CentOS]
    C --&amp;gt;|最新技术| H[Fedora]

    D --&amp;gt;|完全新手| I[Ubuntu/Linux Mint]
    D --&amp;gt;|有一定经验| J[Fedora/openSUSE]
    D --&amp;gt;|高级用户| K[Arch]

    style A fill:#e1f5ff
    style E fill:#fff3e0
    style F fill:#ffebee
&lt;/pre&gt;

&lt;h2 id=&#34;为什么学习-linux&#34;&gt;为什么学习 Linux？
&lt;/h2&gt;&lt;h3 id=&#34;1-服务器领域的绝对统治&#34;&gt;1. 服务器领域的绝对统治
&lt;/h3&gt;&lt;p&gt;根据 StatCounter 和 W3Techs 的统计：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;超级计算机&lt;/strong&gt;：96%+ 运行 Linux&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;公共云&lt;/strong&gt;：90%+ 的云实例运行 Linux&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Web 服务器&lt;/strong&gt;：超过 70% 的网站使用 Linux&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;移动设备&lt;/strong&gt;：Android（基于 Linux）占据 70%+ 市场份额&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;2-开源社区的优势&#34;&gt;2. 开源社区的优势
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;透明性&lt;/strong&gt;：代码公开，可审计&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;安全性&lt;/strong&gt;：全球安全专家持续审查&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;创新性&lt;/strong&gt;：社区驱动，快速迭代&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;免费性&lt;/strong&gt;：大多数发行版免费使用&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;3-开发者必备技能&#34;&gt;3. 开发者必备技能
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;云计算&lt;/strong&gt;：AWS、GCP、Azure 等平台主要使用 Linux&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;容器技术&lt;/strong&gt;：Docker、Kubernetes 基于 Linux&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;DevOps&lt;/strong&gt;：CI/CD 工具链主要运行在 Linux 上&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;后端开发&lt;/strong&gt;：大多数生产环境是 Linux&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;4-深入理解计算机科学&#34;&gt;4. 深入理解计算机科学
&lt;/h3&gt;&lt;p&gt;学习 Linux 让你能够：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;🧠 理解操作系统原理&lt;/li&gt;
&lt;li&gt;🔧 掌握系统级编程&lt;/li&gt;
&lt;li&gt;🚀 优化应用性能&lt;/li&gt;
&lt;li&gt;🐛 高效调试问题&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;5-职业发展&#34;&gt;5. 职业发展
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;系统管理员&lt;/strong&gt;：Linux/Unix 系统管理&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;DevOps 工程师&lt;/strong&gt;：自动化和部署&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;后端工程师&lt;/strong&gt;：服务器端开发&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;嵌入式开发&lt;/strong&gt;：Linux 嵌入式系统&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;安全研究员&lt;/strong&gt;：系统安全和渗透测试&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;linux-的学习路径&#34;&gt;Linux 的学习路径
&lt;/h2&gt;&lt;pre class=&#34;mermaid&#34;&gt;
  flowchart LR
    A[基础命令行] --&amp;gt; B[Shell 脚本编程]
    B --&amp;gt; C[系统管理]
    C --&amp;gt; D[网络基础]
    D --&amp;gt; E[服务部署]
    E --&amp;gt; F[性能优化]

    style A fill:#e8f5e9
    style B fill:#c8e6c9
    style C fill:#a5d6a7
    style D fill:#81c784
    style E fill:#66bb6a
    style F fill:#4caf50
&lt;/pre&gt;

&lt;h3 id=&#34;推荐学习顺序&#34;&gt;推荐学习顺序
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;第一阶段：基础命令&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;文件和目录操作&lt;/li&gt;
&lt;li&gt;权限管理&lt;/li&gt;
&lt;li&gt;文本处理工具&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;第二阶段：Shell 脚本&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Bash 基础&lt;/li&gt;
&lt;li&gt;变量和函数&lt;/li&gt;
&lt;li&gt;流程控制&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;第三阶段：系统管理&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;进程管理&lt;/li&gt;
&lt;li&gt;软件包管理&lt;/li&gt;
&lt;li&gt;系统服务&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;第四阶段：高级主题&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;网络配置&lt;/li&gt;
&lt;li&gt;安全设置&lt;/li&gt;
&lt;li&gt;性能调优&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&#34;实践建议&#34;&gt;实践建议
&lt;/h2&gt;&lt;h3 id=&#34;1-虚拟机实践&#34;&gt;1. 虚拟机实践
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 推荐使用 VirtualBox 或 VMware
# 初学者推荐发行版：
- Ubuntu Desktop（图形界面，易上手）
- Ubuntu Server（命令行，更接近生产环境）&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;2-在线练习平台&#34;&gt;2. 在线练习平台
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;OverTheWire&lt;/strong&gt;：https://overthewire.org/wargames/&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Linux Journey&lt;/strong&gt;：https://linuxjourney.com/&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;实验楼&lt;/strong&gt;：https://www.lanqiao.cn/&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;3-阅读资料&#34;&gt;3. 阅读资料
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;《鸟哥的Linux私房菜》&lt;/strong&gt;：经典入门教材&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;《Linux就该这么学》&lt;/strong&gt;：适合初学者&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;《UNIX环境高级编程》（APUE）&lt;/strong&gt;：进阶必读&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;《深入理解Linux内核》&lt;/strong&gt;：内核开发参考&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;总结&#34;&gt;总结
&lt;/h2&gt;&lt;p&gt;Linux 不仅仅是一个操作系统，它代表了一种开放、协作、共享的精神。从 Linus 的个人项目到统治现代计算世界的操作系统，Linux 的成功证明了开源模式的力量。&lt;/p&gt;
&lt;h3 id=&#34;核心要点&#34;&gt;核心要点
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;历史传承&lt;/strong&gt;：Linux 继承了 Unix 的设计哲学&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;社区力量&lt;/strong&gt;：GNU 项目和全球开发者共同构建了生态系统&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;技术优势&lt;/strong&gt;：单内核 + 模块化设计 = 高性能 + 灵活性&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;广泛应用&lt;/strong&gt;：从嵌入式到超级计算机，无处不在&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;学习价值&lt;/strong&gt;：理解 Linux = 理解现代计算基础&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;下一步&#34;&gt;下一步
&lt;/h3&gt;&lt;p&gt;在这个系列中，我们将深入探索：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Linux 内核架构&lt;/li&gt;
&lt;li&gt;进程和内存管理&lt;/li&gt;
&lt;li&gt;文件系统&lt;/li&gt;
&lt;li&gt;设备驱动&lt;/li&gt;
&lt;li&gt;网络栈&lt;/li&gt;
&lt;li&gt;Shell 编程&lt;/li&gt;
&lt;li&gt;系统管理&lt;/li&gt;
&lt;li&gt;性能优化&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;&amp;ldquo;Talk is cheap. Show me the code.&amp;rdquo; — Linus Torvalds&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;让我们一起深入 Linux 的世界，探索它的奥秘！🚀&lt;/p&gt;
&lt;h2 id=&#34;系列文章导航&#34;&gt;系列文章导航
&lt;/h2&gt;&lt;p&gt;本系列深入探讨 Linux 操作系统的各个方面：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;&lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/linux-%E6%A6%82%E8%BF%B0%E4%BB%8E-unix-%E5%88%B0%E5%BC%80%E6%BA%90%E9%9D%A9%E5%91%BD/&#34; &gt;Linux 概述：从 Unix 到开源革命&lt;/a&gt;&lt;/strong&gt; （当前文章）&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/linux-%E5%86%85%E6%A0%B8%E6%9E%B6%E6%9E%84%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E7%9A%84%E6%A0%B8%E5%BF%83/&#34; &gt;Linux 内核架构：操作系统的核心&lt;/a&gt;&lt;/strong&gt; - 用户空间与内核空间、系统调用、五大子系统&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/%E8%BF%9B%E7%A8%8B%E7%AE%A1%E7%90%86linux-%E7%9A%84%E7%94%9F%E5%91%BD%E7%BA%BF/&#34; &gt;进程管理：Linux 的生命线&lt;/a&gt;&lt;/strong&gt; - 进程生命周期、CFS 调度器、进程监控&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9Flinux-%E7%9A%84%E6%95%B0%E6%8D%AE%E5%AE%AB%E6%AE%BF/&#34; &gt;文件系统：数据的管理者&lt;/a&gt;&lt;/strong&gt; - VFS、Ext4、文件权限与管理&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/shell-%E5%92%8C%E5%91%BD%E4%BB%A4%E8%A1%8Clinux-%E7%9A%84%E6%8E%A7%E5%88%B6%E5%8F%B0/&#34; &gt;Shell 和命令行：Linux 的控制台&lt;/a&gt;&lt;/strong&gt; - Bash 基础、管道重定向、脚本编程&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/%E6%97%A5%E5%BF%97%E5%92%8C%E7%9B%91%E6%8E%A7linux-%E7%9A%84%E4%BD%93%E6%A3%80%E5%8C%BB%E7%94%9F/&#34; &gt;日志与监控：系统的体检报告&lt;/a&gt;&lt;/strong&gt; - 系统日志、性能监控、故障排查&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a class=&#34;link&#34; href=&#34;http://localhost:1313/p/%E8%BD%AF%E4%BB%B6%E5%8C%85%E7%AE%A1%E7%90%86linux-%E7%9A%84%E5%BA%94%E7%94%A8%E5%95%86%E5%BA%97/&#34; &gt;软件包管理：应用的生态圈&lt;/a&gt;&lt;/strong&gt; - APT/DNF/Pacman、软件源、依赖管理&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;p&gt;&lt;strong&gt;参考资源&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.kernel.org/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Linux 内核官网&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.gnu.org/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;GNU 项目官网&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://tldp.org/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Linux Documentation Project&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://wiki.archlinux.org/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Arch Linux Wiki&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://linuxjourney.com/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Linux Journey 在线教程&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
        </item>
        <item>
        <title>SHA 校验入门：从原理到实践</title>
        <link>http://localhost:1313/p/sha-%E6%A0%A1%E9%AA%8C%E5%85%A5%E9%97%A8%E4%BB%8E%E5%8E%9F%E7%90%86%E5%88%B0%E5%AE%9E%E8%B7%B5/</link>
        <pubDate>Fri, 05 Dec 2025 02:31:57 -0500</pubDate>
        
        <guid>http://localhost:1313/p/sha-%E6%A0%A1%E9%AA%8C%E5%85%A5%E9%97%A8%E4%BB%8E%E5%8E%9F%E7%90%86%E5%88%B0%E5%AE%9E%E8%B7%B5/</guid>
        <description>&lt;img src="http://localhost:1313/default.jpeg" alt="Featured image of post SHA 校验入门：从原理到实践" /&gt;&lt;h1 id=&#34;sha-校验入门从原理到实践&#34;&gt;SHA 校验入门：从原理到实践
&lt;/h1&gt;&lt;p&gt;在数字世界中，我们经常需要下载和传输文件。但你如何能确定下载的文件就是发布者想要你收到的那个，没有被篡改，也没有在传输过程中损坏呢？答案就是 &lt;strong&gt;哈希校验（Hash Validation）&lt;/strong&gt;，而 SHA256 正是其中最常用的一种算法。&lt;/p&gt;
&lt;p&gt;这篇笔记将带你从零开始，理解 SHA256 的基本原理，并学会在不同操作系统上进行实际操作。&lt;/p&gt;
&lt;h2 id=&#34;什么是-sha256&#34;&gt;什么是 SHA256？
&lt;/h2&gt;&lt;p&gt;SHA256 (Secure Hash Algorithm 256) 是一种加密哈希函数。你可以把它想象成一个神奇的“指纹生成器”。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;输入&lt;/strong&gt;：任何数字文件，无论是一个 1KB 的文本文件，还是一个 4GB 的电影。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;输出&lt;/strong&gt;：一个由 256 位（即 64 个十六进制字符）组成的独一无二的字符串，这个字符串就是文件的“数字指纹”。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这个“指紋”有几个关键特性：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;唯一性：几乎不可能找到两个不同的文件，它们会生成相同的 SHA256 指纹。&lt;/li&gt;
&lt;li&gt;不可逆性：你无法通过指纹反向推导出原始文件是什么。&lt;/li&gt;
&lt;li&gt;雪崩效应：即使原始文件只改变了微不足道的一点点（比如一个字符或一个像素），生成的指纹也会发生天翻地覆的变化。&lt;/li&gt;
&lt;li&gt;固定长度：无论原始文件多大或多小，生成的指纹长度永远是固定的 64 个字符。&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&#34;为什么要使用-sha256-校验&#34;&gt;为什么要使用 SHA256 校验？
&lt;/h2&gt;&lt;p&gt;最主要的应用场景是 确保文件的完整性和真实性。&lt;/p&gt;
&lt;blockquote class=&#34;alert alert-tip&#34;&gt;
    &lt;p&gt;当你从网上下载一个操作系统镜像、一个重要的软件安装包或者一份关键文档时，发布者通常会一并提供该文件的 SHA256 哈希值（Checksum）。你下载文件后，可以在本地计算它的哈希值，然后与官方提供的值进行比对。如果两者完全一致，就说明你的文件是“原装正品”，可以放心使用。&lt;/p&gt;&lt;/blockquote&gt;
&lt;h2 id=&#34;如何在不同平台使用-sha256&#34;&gt;如何在不同平台使用 SHA256？
&lt;/h2&gt;&lt;p&gt;下面是在主流操作系统上计算文件 SHA256 哈希值的实用命令。&lt;/p&gt;
&lt;h3 id=&#34;linux&#34;&gt;Linux
&lt;/h3&gt;&lt;p&gt;在大多数 Linux 发行版中，&lt;code&gt;sha256sum&lt;/code&gt; 是一个预装的命令行工具，非常方便。&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;sha256sum /path/to/your/file

# 通过哈希值直接验证下载后的文件完整性
echo &amp;#39;&amp;lt;hash_code&amp;gt; &amp;lt;file_name&amp;gt;&amp;#39; | sha256sum -c&amp;#39;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;blockquote class=&#34;alert alert-tip&#34;&gt;
    &lt;p&gt;一般情况下gihub上release文件都自带sha256可以直接校验.&lt;/p&gt;&lt;/blockquote&gt;
&lt;p&gt;&lt;strong&gt;示例：&lt;/strong&gt;
假设我们下载了一个名为 &lt;code&gt;ubuntu-24.04-desktop-amd64.iso&lt;/code&gt; 的文件。&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 计算 ISO 文件的 SHA256 哈希值
sha256sum ubuntu-24.04-desktop-amd64.iso

# 命令执行后会输出类似下面的结果
# 5b863375b70b5711d51c35068205562e316a3c64c4d5b6f3f7756f6b5f5a8f3d  ubuntu-24.04-desktop-amd64.iso&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;你只需要将输出的这串 &lt;code&gt;5b86...&lt;/code&gt; 与官方网站提供的哈希值进行对比即可。&lt;/p&gt;
&lt;h3 id=&#34;macos&#34;&gt;macOS
&lt;/h3&gt;&lt;p&gt;macOS 用户可以使用 &lt;code&gt;shasum&lt;/code&gt; 命令，并通过 &lt;code&gt;-a&lt;/code&gt; 参数指定算法。&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;shasum -a 256 /path/to/your/file&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;示例：&lt;/strong&gt;&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# 计算同一个 ISO 文件的哈希值
shasum -a 256 ubuntu-24.04-desktop-amd64.iso

# 输出格式与 Linux 类似
# 5b863375b70b5711d51c35068205562e316a3c64c4d5b6f3f7756f6b5f5a8f3d  ubuntu-24.04-desktop-amd64.iso&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;windows&#34;&gt;Windows
&lt;/h3&gt;&lt;p&gt;Windows 用户可以使用系统内置的 &lt;code&gt;CertUtil&lt;/code&gt; 工具。&lt;/p&gt;





&lt;div class=&#34;highlight highlight-powershell&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-powershell&#34; data-lang=&#34;powershell&#34;&gt;CertUtil -hashfile C:\path\to\your\file SHA256&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;示例：&lt;/strong&gt;&lt;/p&gt;





&lt;div class=&#34;highlight highlight-powershell&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-powershell&#34; data-lang=&#34;powershell&#34;&gt;# 在 PowerShell 或 CMD 中运行
CertUtil -hashfile D:\Downloads\ubuntu-24.04-desktop-amd64.iso SHA256

# 输出会是这样的格式
# SHA256 哈希值:
# 5b863375b70b5711d51c35068205562e316a3c64c4d5b6f3f7756f6b5f5a8f3d
# CertUtil: -hashfile 命令成功完成。&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;blockquote class=&#34;alert alert-note&#34;&gt;
    &lt;p&gt;Windows 输出的哈希值默认没有空格，但某些第三方工具可能会添加空格。比对时请确保格式一致，或者忽略空格进行比较。&lt;/p&gt;&lt;/blockquote&gt;
&lt;h2 id=&#34;总结&#34;&gt;总结
&lt;/h2&gt;&lt;p&gt;SHA256 校验是保护我们免受损坏文件和恶意软件侵害的一个简单而强大的工具。养成在处理重要文件时进行哈希校验的习惯，将为你的数字生活增添一道重要的安全防线。操作本身非常简单，几条命令就能搞定，但其背后的安全意义却十分重大。&lt;/p&gt;
</description>
        </item>
        <item>
        <title>Apt</title>
        <link>http://localhost:1313/p/apt/</link>
        <pubDate>Fri, 05 Dec 2025 01:27:47 -0500</pubDate>
        
        <guid>http://localhost:1313/p/apt/</guid>
        <description>&lt;img src="http://localhost:1313/default.jpeg" alt="Featured image of post Apt" /&gt;&lt;h2 id=&#34;reference&#34;&gt;Reference
&lt;/h2&gt;&lt;div class=&#34;video-wrapper&#34;&gt;
    &lt;iframe loading=&#34;lazy&#34; 
            src=&#34;https://www.youtube.com/embed/j3fOfBwom5k&#34; 
            allowfullscreen 
            title=&#34;YouTube Video&#34;
    &gt;
    &lt;/iframe&gt;
&lt;/div&gt;

&lt;h2 id=&#34;hook&#34;&gt;Hook
&lt;/h2&gt;&lt;p&gt;Imagine you&amp;rsquo;re trying to install a new software on your Linux system, but you have to manually download packages, resolve dependencies, and configure everything by hand. This process can quickly become overwhelming and error-prone. The Advanced Package Tool (APT) was created to solve these problems by providing an easy and automated way to manage software packages on Debian-based systems.&lt;/p&gt;
&lt;h2 id=&#34;feature&#34;&gt;Feature
&lt;/h2&gt;&lt;p&gt;APT (Advanced Package Tool) is a powerful command-line package management system used in Debian and its derivatives like Ubuntu. It simplifies the process of installing, updating, and removing software packages while automatically handling dependencies. APT maintains a database of available packages from configured repositories and ensures system integrity by resolving dependencies and conflicts.&lt;/p&gt;
&lt;h2 id=&#34;reference-1&#34;&gt;Reference
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://wiki.debian.org/Apt&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Debian APT Documentation&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://help.ubuntu.com/community/AptGet/Howto&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Ubuntu APT Guide&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;quick-start&#34;&gt;Quick Start
&lt;/h2&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Update package index (downloads latest package information)
sudo apt update

# Upgrade all installed packages to their latest versions
sudo apt upgrade

# Install a package / include local deb docs
sudo apt install &amp;lt;package_name&amp;gt;

# Remove a package (keeps configuration files)
sudo apt remove &amp;lt;package_name&amp;gt;

# Completely remove a package and its configuration files
sudo apt purge &amp;lt;package_name&amp;gt;

# Search for packages
apt search &amp;lt;keyword&amp;gt;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;common-commands&#34;&gt;Common Commands
&lt;/h2&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Update package list and upgrade all packages
sudo apt update &amp;amp;&amp;amp; sudo apt upgrade -y

# Install multiple packages
sudo apt install package1 package2 package3

# Install a package without confirmation prompts
sudo apt install -y &amp;lt;package_name&amp;gt;

# Show package information
apt show &amp;lt;package_name&amp;gt;

# List all installed packages
apt list --installed

# List upgradable packages
apt list --upgradable

# Remove unused dependencies
sudo apt autoremove

# Clean package cache (removes downloaded .deb files)
sudo apt clean

# Clean package cache (removes outdated .deb files)
sudo apt autoclean&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;blockquote class=&#34;alert alert-note&#34;&gt;
    &lt;p&gt;Always run &lt;code&gt;sudo apt update&lt;/code&gt; before installing new packages to ensure you&amp;rsquo;re getting the latest versions.&lt;/p&gt;&lt;/blockquote&gt;
&lt;h2 id=&#34;advanced-commands--features&#34;&gt;Advanced Commands &amp;amp; Features
&lt;/h2&gt;&lt;h3 id=&#34;package-search-and-information&#34;&gt;Package Search and Information
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Search for packages by name or description
apt search &amp;lt;keyword&amp;gt;

# Show detailed information about a package
apt show &amp;lt;package_name&amp;gt;

# List all available versions of a package
apt list -a &amp;lt;package_name&amp;gt;

# Show installed packages matching a pattern
apt list --installed | grep &amp;lt;pattern&amp;gt;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;package-installation-options&#34;&gt;Package Installation Options
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Install a specific version of a package
sudo apt install &amp;lt;package_name&amp;gt;=&amp;lt;version&amp;gt;

# Reinstall a package
sudo apt reinstall &amp;lt;package_name&amp;gt;

# Simulate an installation (dry run)
sudo apt install --dry-run &amp;lt;package_name&amp;gt;

# Install recommended packages automatically
sudo apt install --install-recommends &amp;lt;package_name&amp;gt;

# Install without recommended packages
sudo apt install --no-install-recommends &amp;lt;package_name&amp;gt;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;system-maintenance&#34;&gt;System Maintenance
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Full system upgrade (may remove/install packages)
sudo apt full-upgrade

# Fix broken dependencies
sudo apt --fix-broken install

# Fix missing packages
sudo apt --fix-missing update

# Download package files without installing
sudo apt download &amp;lt;package_name&amp;gt;

# Check for broken dependencies
sudo apt check&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;repository-management&#34;&gt;Repository Management
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Add a repository
sudo add-apt-repository &amp;lt;repository&amp;gt;

# Add a repository without prompting
sudo add-apt-repository -y &amp;lt;repository&amp;gt;

# Remove a repository
sudo add-apt-repository --remove &amp;lt;repository&amp;gt;

# List all configured repositories
apt policy

# Update package list for a specific repository
sudo apt update -o Dir::Etc::sourcelist=&amp;#34;sources.list.d/&amp;lt;repo&amp;gt;.list&amp;#34; \
                -o Dir::Etc::sourceparts=&amp;#34;-&amp;#34; \
                -o APT::Get::List-Cleanup=&amp;#34;0&amp;#34;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h3 id=&#34;package-pinning-holding-package-versions&#34;&gt;Package Pinning (Holding Package Versions)
&lt;/h3&gt;




&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Hold a package at its current version (prevent upgrades)
sudo apt-mark hold &amp;lt;package_name&amp;gt;

# Unhold a package (allow upgrades again)
sudo apt-mark unhold &amp;lt;package_name&amp;gt;

# Show held packages
apt-mark showhold

# Mark a package as automatically installed
sudo apt-mark auto &amp;lt;package_name&amp;gt;

# Mark a package as manually installed
sudo apt-mark manual &amp;lt;package_name&amp;gt;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;configuration-files&#34;&gt;Configuration Files
&lt;/h2&gt;&lt;p&gt;APT configuration files are located in &lt;code&gt;/etc/apt/&lt;/code&gt;:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;/etc/apt/sources.list&lt;/code&gt; - Main repository configuration&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/etc/apt/sources.list.d/&lt;/code&gt; - Additional repository files&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/etc/apt/apt.conf&lt;/code&gt; - Main APT configuration&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/etc/apt/apt.conf.d/&lt;/code&gt; - Configuration snippets&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/etc/apt/preferences&lt;/code&gt; - Package pinning preferences&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/etc/apt/preferences.d/&lt;/code&gt; - Additional pinning files&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;best-practices&#34;&gt;Best Practices
&lt;/h2&gt;&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Regular Updates&lt;/strong&gt;: Run &lt;code&gt;sudo apt update&lt;/code&gt; regularly to keep package information current&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;System Upgrades&lt;/strong&gt;: Use &lt;code&gt;sudo apt full-upgrade&lt;/code&gt; instead of &lt;code&gt;upgrade&lt;/code&gt; for major system updates&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Clean Cache&lt;/strong&gt;: Periodically run &lt;code&gt;sudo apt autoclean&lt;/code&gt; to free disk space&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Remove Unused Packages&lt;/strong&gt;: Run &lt;code&gt;sudo apt autoremove&lt;/code&gt; to remove unnecessary dependencies&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Backup Configuration&lt;/strong&gt;: Before major upgrades, backup important configuration files&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Check Before Installing&lt;/strong&gt;: Use &lt;code&gt;apt show&lt;/code&gt; to review package details before installation&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&#34;troubleshooting&#34;&gt;Troubleshooting
&lt;/h2&gt;&lt;h3 id=&#34;common-issues-and-solutions&#34;&gt;Common Issues and Solutions
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;GPG Errors&lt;/strong&gt;:&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Fix missing GPG keys
sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys &amp;lt;KEY_ID&amp;gt;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Repository Issues&lt;/strong&gt;:&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Fix repository errors
sudo apt clean
sudo apt update --fix-missing&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Broken Packages&lt;/strong&gt;:&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Fix broken dependencies
sudo apt --fix-broken install
sudo dpkg --configure -a&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Lock Errors&lt;/strong&gt;:&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Remove lock files (only if no other apt process is running)
sudo rm /var/lib/apt/lists/lock
sudo rm /var/cache/apt/archives/lock
sudo rm /var/lib/dpkg/lock*&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;/li&gt;
&lt;/ol&gt;
&lt;blockquote class=&#34;alert alert-tip&#34;&gt;
    &lt;p&gt;Always be cautious when removing lock files. Make sure no other package manager is running.&lt;/p&gt;&lt;/blockquote&gt;
</description>
        </item>
        <item>
        <title>Soft_links</title>
        <link>http://localhost:1313/p/soft_links/</link>
        <pubDate>Thu, 31 Jul 2025 15:38:14 +0800</pubDate>
        
        <guid>http://localhost:1313/p/soft_links/</guid>
        <description>&lt;img src="http://localhost:1313/p/soft_links/link.png" alt="Featured image of post Soft_links" /&gt;&lt;h2 id=&#34;软连接&#34;&gt;软连接
&lt;/h2&gt;&lt;p&gt;在系统中我们往往可能需要在一个项目中使用项目之外的一些文件，但是这个文件又不在这项目的根目录在。大部分人往往都选择直接将需要的文件拷贝到项目的路目中。但是这样既浪费时间又浪费系统的存储空间。尔软连接的出现就是为了解决这一问题。我们可以直接通过命令创建一个软连接。让目标文件或者文件夹的替身出现在我们的项目目录下。这样我们就可以在不使用绝对路径的情况下使用该路径了。&lt;/p&gt;
&lt;h3 id=&#34;如何使用软连接ln&#34;&gt;如何使用软连接（ln
&lt;/h3&gt;&lt;p&gt;在 linux 中软连接的是由&amp;rsquo;ln&amp;rsquo;这一指令完成的。&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;在 linux 我们可以使用&amp;rsquo;man ln&amp;rsquo;就能查看 ln 的使用手册&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;这里列举一些常用的&amp;rsquo;ln&amp;rsquo; 命令&lt;/p&gt;





&lt;div class=&#34;highlight highlight-bash&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;# Create a symbolic link to a file or directory:
# 会在路标路径下创建一个原路径同名的文件或者文件夹
ln --symbolic /path/to/file_or_directory path/to/symlink

#Overwrite an existing symbolic link to point to a different file:
ln --symbolic --force /path/to/new_file path/to/symlink

# Create a hard link to a file:
ln /path/to/file path/to/hardlink

# Delete a symbolic link:
rm path/to/symlink&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;blockquote class=&#34;alert alert-tips&#34;&gt;
    &lt;p&gt;如果链接是相对路径，那这个相对是路径应该是相对于软连接目标目录的路径
###&lt;a class=&#34;link&#34; href=&#34;https://www.gnu.org/software/coreutils/manual/html_node/ln-invocation.html&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;官方文档链接&lt;/a&gt;&lt;/p&gt;&lt;/blockquote&gt;
</description>
        </item>
        <item>
        <title>Linux_system_service | Linux 之 系统服务</title>
        <link>http://localhost:1313/p/linux_system_service-linux-%E4%B9%8B-%E7%B3%BB%E7%BB%9F%E6%9C%8D%E5%8A%A1/</link>
        <pubDate>Fri, 13 Jun 2025 15:47:20 +0800</pubDate>
        
        <guid>http://localhost:1313/p/linux_system_service-linux-%E4%B9%8B-%E7%B3%BB%E7%BB%9F%E6%9C%8D%E5%8A%A1/</guid>
        <description>&lt;img src="http://localhost:1313/p/linux_system_service-linux-%E4%B9%8B-%E7%B3%BB%E7%BB%9F%E6%9C%8D%E5%8A%A1/image.png" alt="Featured image of post Linux_system_service | Linux 之 系统服务" /&gt;&lt;h2 id=&#34;什么是系统服务service&#34;&gt;什么是系统服务(service)
&lt;/h2&gt;&lt;p&gt;简单理解就是开机自启动的程序或者脚本, 会常驻在系统的内存中.&lt;/p&gt;
&lt;p&gt;对于现在的大部分的 linux 发行版, 系统服务通常使用 systemd 进行管理.&lt;/p&gt;
&lt;h3 id=&#34;三种配置文件&#34;&gt;三种配置文件
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;系统:&lt;/li&gt;
&lt;/ol&gt;
&lt;ul&gt;
&lt;li&gt;dir: &lt;code&gt;/usr/lib/systemd/system/&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;这里存放着系统自带的服务单元, 如 apt. 一般不建议随便乱动这里的服务, 除非你知道自己在干什么.&lt;/li&gt;
&lt;/ul&gt;
&lt;ol start=&#34;2&#34;&gt;
&lt;li&gt;运行时:&lt;/li&gt;
&lt;/ol&gt;
&lt;ul&gt;
&lt;li&gt;dir: &lt;code&gt;/run/systemd/system/&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;临时服务, 一般可其他服务或者用户行为启动的临时服务. 在系统重启后清空&lt;/li&gt;
&lt;/ul&gt;
&lt;ol start=&#34;3&#34;&gt;
&lt;li&gt;管理员:&lt;/li&gt;
&lt;/ol&gt;
&lt;ul&gt;
&lt;li&gt;dir: &lt;code&gt;/etc/systemd/system/&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;用户自定义的服务, 管理员系统服务优先级高于系统服务, 意味着它会覆盖系统中相同的服务.&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote class=&#34;alert alert-note&#34;&gt;
    &lt;p&gt;如果我们想添加自己的系统服务, 建议都放在管理员服务目录下.&lt;/p&gt;&lt;/blockquote&gt;
&lt;h2 id=&#34;service-文件&#34;&gt;.service 文件
&lt;/h2&gt;&lt;h3 id=&#34;为什么要定义这种文件格式&#34;&gt;为什么要定义这种文件格式
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;用于描述该服务的内容, 配置, 策略等&lt;/li&gt;
&lt;li&gt;可以使用&lt;code&gt;systemctl&lt;/code&gt;命令来管理服务.&lt;/li&gt;
&lt;li&gt;统一服务的管理.&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&#34;核心结构&#34;&gt;核心结构
&lt;/h3&gt;&lt;h4 id=&#34;unit&#34;&gt;&lt;code&gt;Unit&lt;/code&gt;
&lt;/h4&gt;&lt;p&gt;用于定义服务的元信息(meta data)和依赖.&lt;/p&gt;





&lt;div class=&#34;highlight highlight-service&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-service&#34; data-lang=&#34;service&#34;&gt;[Unit]
Description=My Service
Documentation=https://example.org/docs/my-service
After=network.target #这里需要定义这个服务在哪个服务之后启动
Wants=network.target #弱依赖
Requires=network.target #强依赖&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;常见的系统服务目标（可以在 After 中使用）：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;basic.target&lt;/code&gt;: 基础系统初始化完成&lt;/li&gt;
&lt;li&gt;&lt;code&gt;network.target&lt;/code&gt;: 网络已启动&lt;/li&gt;
&lt;li&gt;&lt;code&gt;multi-user.target&lt;/code&gt;: 多用户模式（常规启动模式）&lt;/li&gt;
&lt;li&gt;&lt;code&gt;graphical.target&lt;/code&gt;: 图形界面模式&lt;/li&gt;
&lt;li&gt;&lt;code&gt;remote-fs.target&lt;/code&gt;: 远程文件系统已挂载&lt;/li&gt;
&lt;li&gt;&lt;code&gt;syslog.target&lt;/code&gt;: 系统日志服务&lt;/li&gt;
&lt;li&gt;&lt;code&gt;time-sync.target&lt;/code&gt;: 系统时间已同步&lt;/li&gt;
&lt;li&gt;&lt;code&gt;nss-lookup.target&lt;/code&gt;: 网络名称解析服务&lt;/li&gt;
&lt;li&gt;&lt;code&gt;sound.target&lt;/code&gt;: 声音系统&lt;/li&gt;
&lt;li&gt;&lt;code&gt;bluetooth.target&lt;/code&gt;: 蓝牙服务&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;也可以直接指定其他具体的服务，例如：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-service&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-service&#34; data-lang=&#34;service&#34;&gt;After=mysql.service redis.service&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;服务依赖关系选项：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Requires=&lt;/code&gt;: 强依赖，如果依赖的服务启动失败，该服务也会失败&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Wants=&lt;/code&gt;: 弱依赖，依赖服务的启动状态不影响本服务&lt;/li&gt;
&lt;li&gt;&lt;code&gt;BindsTo=&lt;/code&gt;: 绑定依赖，依赖服务停止时，该服务也会停止&lt;/li&gt;
&lt;li&gt;&lt;code&gt;PartOf=&lt;/code&gt;: 部分依赖，依赖服务停止或重启时，该服务也会停止或重启&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Conflicts=&lt;/code&gt;: 冲突依赖，与指定服务冲突，不能同时运行&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Before=&lt;/code&gt;: 在指定服务之前启动&lt;/li&gt;
&lt;li&gt;&lt;code&gt;After=&lt;/code&gt;: 在指定服务之后启动&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;示例：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-service&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-service&#34; data-lang=&#34;service&#34;&gt;Wants=redis.service
After=network.target redis.service
Conflicts=apache2.service&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;service&#34;&gt;&lt;code&gt;Service&lt;/code&gt;
&lt;/h4&gt;&lt;p&gt;定义服务的行为&lt;/p&gt;





&lt;div class=&#34;highlight highlight-service&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-service&#34; data-lang=&#34;service&#34;&gt;[Service]
Type = simple
ExecStart = /usr/bin/redis-server /etc/redis/redis.conf
Restart = always
User = redis
Group = redis
Environment = e.g. PATH=/usr/bin
WorkingDirectory = /etc/redis&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;Service 块的主要配置项：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;Type&lt;/code&gt;: 服务的启动类型&lt;/li&gt;
&lt;/ol&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;simple&lt;/code&gt;: 默认值，ExecStart 启动的进~程为主进程，启动后会持续运行。适用于持续运行的服务，如 web 服务器、数据库等。
示例：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-service&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-service&#34; data-lang=&#34;service&#34;&gt;[Service]
Type=simple
ExecStart=/usr/bin/nginx -g &amp;#39;daemon off;&amp;#39;&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;oneshot&lt;/code&gt;: 一次性进程，执行完就退出。适用于只需要执行一次的任务，如系统初始化脚本。通常需要配合 &lt;code&gt;RemainAfterExit=yes&lt;/code&gt; 使用，表示进程退出后服务仍然处于活动状态。比如我要执行一个脚本, 那么我可以通过关闭这个服务来关闭这个脚本.
示例：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-service&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-service&#34; data-lang=&#34;service&#34;&gt;[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/local/bin/init-script.sh&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;forking&lt;/code&gt;: 服务会 fork，父进程退出后服务启动成功&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;notify&lt;/code&gt;: 服务启动完成后会通知 systemd&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;dbus&lt;/code&gt;: 通过 D-Bus 启动&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;idle&lt;/code&gt;: 等待其他任务处理完成后才启动~&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;ol start=&#34;2&#34;&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;ExecStart&lt;/code&gt;: 启动服务时执行的命令&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;ExecStop&lt;/code&gt;: 停止服务时执行的命令&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;ExecReload&lt;/code&gt;: 重载服务时执行的命令&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;Restart&lt;/code&gt;: 服务重启策略&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;no&lt;/code&gt;: 不自动重启&lt;/li&gt;
&lt;li&gt;&lt;code&gt;always&lt;/code&gt;: 总是重启&lt;/li&gt;
&lt;li&gt;&lt;code&gt;on-success&lt;/code&gt;: 正常退出时重启&lt;/li&gt;
&lt;li&gt;&lt;code&gt;on-failure&lt;/code&gt;: 非正常退出时重启&lt;/li&gt;
&lt;li&gt;&lt;code&gt;on-abnormal&lt;/code&gt;: 被信号终止或超时时重启&lt;/li&gt;
&lt;li&gt;&lt;code&gt;on-abort&lt;/code&gt;: 收到未处理信号退出时重启&lt;/li&gt;
&lt;/ul&gt;
&lt;ol start=&#34;6&#34;&gt;
&lt;li&gt;权限相关：&lt;/li&gt;
&lt;/ol&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;User&lt;/code&gt;: 以某个用户身份运行&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Group&lt;/code&gt;: 以某个用户组运行&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Nice&lt;/code&gt;: 进程优先级，数值范围为 -20 到 19&lt;/li&gt;
&lt;/ul&gt;
&lt;ol start=&#34;7&#34;&gt;
&lt;li&gt;环境相关：&lt;/li&gt;
&lt;/ol&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Environment&lt;/code&gt;: 设置环境变量&lt;/li&gt;
&lt;li&gt;&lt;code&gt;EnvironmentFile&lt;/code&gt;: 环境变量配置文件&lt;/li&gt;
&lt;li&gt;&lt;code&gt;WorkingDirectory&lt;/code&gt;: 工作目录&lt;/li&gt;
&lt;/ul&gt;
&lt;ol start=&#34;8&#34;&gt;
&lt;li&gt;
&lt;p&gt;资源限制：&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;CPU 相关限制：&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;CPUQuota&lt;/code&gt;: CPU 时间配额限制，以百分比表示。100% 表示一个逻辑处理器的全部时间。例如：&lt;/li&gt;
&lt;li&gt;&lt;code&gt;CPUQuota=100%&lt;/code&gt;: 限制使用一个逻辑处理器的全部时间&lt;/li&gt;
&lt;li&gt;&lt;code&gt;CPUWeight&lt;/code&gt;: CPU 权重，范围 1-10000，默认 100&lt;/li&gt;
&lt;li&gt;&lt;code&gt;CPUAffinity&lt;/code&gt;: 指定进程可以运行在哪些 CPU 核心上，例如 &lt;code&gt;CPUAffinity=0,1,2&lt;/code&gt; 表示只能在前三个核心上运行&lt;/li&gt;
&lt;/ul&gt;
&lt;ol start=&#34;2&#34;&gt;
&lt;li&gt;&lt;code&gt;MemoryLimit&lt;/code&gt;: 内存使用限制，支持单位 K、M、G、T，如 &lt;code&gt;MemoryLimit=1G&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;LimitNOFILE&lt;/code&gt;: 文件描述符限制，如 &lt;code&gt;LimitNOFILE=65535&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;LimitNPROC&lt;/code&gt;: 进程数限制，如 &lt;code&gt;LimitNPROC=4096&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;TasksMax&lt;/code&gt;: 任务数限制，如 &lt;code&gt;TasksMax=100&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;IOWeight&lt;/code&gt;: IO 权重，范围 1-10000，默认 100&lt;/li&gt;
&lt;li&gt;&lt;code&gt;BlockIOWeight&lt;/code&gt;: 块设备 IO 权重，范围 10-1000&lt;/li&gt;
&lt;li&gt;&lt;code&gt;DeviceAllow&lt;/code&gt;: 设备访问控制，如 &lt;code&gt;DeviceAllow=/dev/sda r&lt;/code&gt; 表示只读访问&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;示例：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-service&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-service&#34; data-lang=&#34;service&#34;&gt;[Service]
CPUQuota=200%
CPUWeight=500
CPUAffinity=0,1
MemoryLimit=500M
LimitNOFILE=65535
TasksMax=200
IOWeight=500&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h4 id=&#34;install&#34;&gt;&lt;code&gt;Install&lt;/code&gt;
&lt;/h4&gt;&lt;p&gt;定义如何将服务安装到系统，主要用于配置服务的自启动行为。常用配置项：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;WantedBy&lt;/code&gt;: 服务被启用时创建的符号链接的目标。最常用的值是：
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;multi-user.target&lt;/code&gt;: 多用户命令行模式&lt;/li&gt;
&lt;li&gt;&lt;code&gt;graphical.target&lt;/code&gt;: 图形界面模式&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;code&gt;RequiredBy&lt;/code&gt;: 与 WantedBy 类似，但创建强依赖关系&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Also&lt;/code&gt;: 同时启用或停用的其他单元&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Alias&lt;/code&gt;: 为服务创建别名&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;示例：&lt;/p&gt;





&lt;div class=&#34;highlight highlight-service&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-service&#34; data-lang=&#34;service&#34;&gt;[Install]
WantedBy=multi-user.target
Alias=myapp.service
Also=myapp-logger.service&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;服务安装相关命令：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;systemctl enable &amp;lt;服务名&amp;gt;&lt;/code&gt;: 启用服务（开机自启）&lt;/li&gt;
&lt;li&gt;&lt;code&gt;systemctl disable &amp;lt;服务名&amp;gt;&lt;/code&gt;: 停用服务&lt;/li&gt;
&lt;li&gt;&lt;code&gt;systemctl is-enabled &amp;lt;服务名&amp;gt;&lt;/code&gt;: 检查服务是否启用&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;我的-mc-服务器系统服务实例&#34;&gt;我的 mc 服务器系统服务实例
&lt;/h2&gt;




&lt;div class=&#34;highlight highlight-service&#34;&gt;
  &lt;pre&gt;
    &lt;code class=&#34;language-service&#34; data-lang=&#34;service&#34;&gt;[Unit]
Description=Minecraft Server
After=network.target

[Service]
Type=oneshot
User=m
CPUQuota=10000%
WorkingDirectory=/home/m/桌面/mc_server
ExecStart=/bin/sh run.sh
ExecStop=/bin/sh run.sh stop
RemainAfterExit=yes
Restart=on-failure

[Install]
WantedBy=multi-user.target&lt;/code&gt;
  &lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&#34;服务操作--service-operation&#34;&gt;服务操作 | Service Operation
&lt;/h2&gt;&lt;ol&gt;
&lt;li&gt;启动服务 &lt;code&gt;systemctl start mc.service&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;开机启动服务 &lt;code&gt;systemctl enable mc.service&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;查看服务状态 &lt;code&gt;systemctl status mc.service&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;停止服务 &lt;code&gt;systemctl stop mc.service&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
</description>
        </item>
        
    </channel>
</rss>
